# AGENTS.md — ice-trading-chart

## 这是什么

**交易图表库**：K 线、影线命中、OHLC 提示框、含影线的价格轴量程。

依赖方向严格单向：

```
ice-trading-chart  →  ice-chart  →  ice-render
```

读者请先读 `../ice-render/AGENTS.md`（引擎铁律）与 `../ice-chart/AGENTS.md`（图表层约定），
本文件只记**交易层特有**的约定与踩过的坑。

## 铁律

1. **本包是交易语义的唯一落点**。`ice-chart` 保持通用、零金融语义；任何「给 ice-chart
   加个金融扩展点」的思路都不成立 —— 该加的东西一律加在本包。
2. **不得反向依赖**：`ice-chart` 的 `package.json` / 源码里**永远不出现** `ice-trading-chart`。
   本包对上游的改动只能有两类：① 发现上游 bug（通用问题，纯通用修法）；② 上游新增
   **不含交易语义**的通用能力。两类都要先跟用户确认再动上游。
3. **只吃上游的公开导出面**（`@damoqiongqiu/ice-chart` 的 `src/index.ts` 与
   `ice-render` 的入口）。**不许深引** `ice-chart/src/**`、`internal.ts`、
   或任何 `dist/` 里的深层路径 —— 那会把「上游可以自由重构」这件事偷偷取消掉。
4. **数据、量程、提示框都自己实现**，不向上游要钩子：
   - 四个价从 `point.raw` 自己解析（`ohlc.ts`），不依赖 OHLC 内部字段；
   - 价格轴量程走公开的 `yAxis.min` / `yAxis.max`（`axisRange.ts`）；
   - OHLC 提示框走公开的 `tooltip.formatter`（`tooltip.ts`）；
   - 时间轴用 `xAxis: { type: 'category' }`（等宽 K 线、跳过非交易时段）。
5. **读不出的数据项不画**，绝不补 0 或补前值 —— 假 K 线比空缺危险。
6. **不写第三方 / 竞品项目名**（家族铁律）：源码注释、文档、示例页、测试、提交信息里
   都不许出现。要描述视觉或交互，就**描述结果**，不描述来源。
7. **成员顺序**（2026-09-17 定，全家族同口径）：`static 常量/字段 → static 方法 →
   实例字段 → 构造函数 → 访问器 / 实例方法`，即正则 `S*T*F*C*(A|M)*`。示例页的棘轮
   （`tests/examplesConvention.test.ts`）会守住这条。

## 与上游的接法（改代码前必读）

`candlestick` 在本包是**自定义系列**：靠 `registerSeriesType('candlestick', …)` 接进
ice-chart 的系列注册表（`src/register.ts`，幂等）。因此：

- 命中判定、悬停高亮、提示框、图例、序列化、跨图联动**全部走 ice-chart 既有链路**，
  本包不需要自己实现任何一个；
- 用户的 option 里仍然写 `type: 'candlestick'` —— 这个名字在 ice-chart 0.28 之前是内置的，
  本包沿用它，用户的配置不用改；
- 装的是**还没摘掉内置 candlestick 的旧版 ice-chart**（≤0.27.x）时，`registerTradingSeries`
  会检测到已有工厂并沿用内置实现，不会去覆盖内置类型（上游对内置名是抛错的）。

`this.series.option` 是**原始 option 对象的引用**（ice-chart 归一化时不拷贝），所以本包自己的
扩展字段（`candle` 与四个 `*Field`）不需要上游认识它们，就能一路到达系列组件与提示框。
`toSerializableOption` 也是深度拷贝、保留所有键，所以它们同样能进快照。

## 类目轴的两个硬约束（做交易图表必读）

1. **类目是按字符串去重的**。`buildCategoryValues` 用 `String(value)` 做键，所以
   「同一个标签出现两次」会把两根 K 线塌缩到同一个类目上 —— 表面看是「K 线叠在一起」，
   实际是命中判定、十字光标、抬头全部错位。**多天数据的标签必须带日期**：跨度 ≥ 24h 时
   写 `MM-DD HH:MM`，1D 写 `MM-DD`。示例页的 `keyOf()` 就是这么做的（「露多少」是另一件事，
   交给 `displayLabel()`）。
2. **`BandScale.indexOf` 刻意不做「数值当类目下标」的退化**（`BandScale.ts:44-47`）。
   成交量这类「派生系列」的数据项必须**带上和 K 线相同的 x**，只写 `{ value, color }`
   在类目轴上会映射到 NaN、柱子整片消失。**「这一根读不出量」的项也要带 x**：
   类目轴是按「这份系列里出现过的 x」建域的，少一个 x 就少一格 —— 中间空一根会让后面
   所有量柱左移一格；尾巴上连着一片空（视窗停在最新 K 右边就是这个情况）则整段类目缺失，
   联动窗口在这里找不到 key，量图会被引擎的 clamp 拉成整幅（实测踩到）。

## K 线列存（改渲染 / 命中前必读）

四个价只解析一次，落成列（`src/series/candleColumns.ts`）：`open/close/low/high` 四条
`Float64Array` + `valid`（读不出的根不画）+ 类目索引表 + 影线极值。**渲染 / 命中 / 价格轴量程
共用同一份**（`candleColumns(data, option)`，WeakMap 按「数据数组 + 字段名签名 + 长度」缓存）。

- **热路径只读标量、只碰可见窗口**：`visibleRange()` 用「类目轴 domain 的首末类目 → 下标」
  两次查表把窗口夹出来（O(1)），绘制与命中都在窗口内（两端各留 2 根）。
  改之前是「命中扫全量 + 每根建一个 Rect」「每帧每根重新 `readOhlc(raw)`」——
  10 万根实测命中 p50 **223ms**、视窗变化 **254ms**；改后是 **0.00ms / 16.7ms**。
- **压缩模式**：一屏超过「每根 0.25 像素」时改画**每像素列一根高低线**（值取该列真实极值，
  颜色取该列最后一根的涨跌）。聚合的是真实极值，不是补值 —— 与「读不出的根不画」同一条纪律。
  桶号按下标比例算，**不要**在压缩循环里调 `xScale.map()`：类目轴的 `map` 是线性查类目，
  10 万根逐根调它 = 首帧 3.7s（实测），按比例算是 0.2ms。
- **列存不是第二份真相**：它由原始数据派生，`data` 换数组 / 变长度会自动重建；
  **等长原地改值**（`data[i].c = ...`）不会被发现 —— 应用要调 `invalidateCandleColumns(data)`。
- **公开的单根几何**：`candleRectAt(i)`（绘制 / 命中 / 外壳读数共用同一套设备像素对齐），
  `visibleRange(pad)` 公开给外壳与审计断言用。
- **不要用 `seriesComponent.pixelAt()`** 做外壳定位（动画期间是补间位置）—— 这条没变，见下一节。
- **K 线默认开上游的列存**（`toTradingOption` 里给 candle 系列加 `virtual: true`，
  用户显式写 `virtual: false` 时不覆盖）：ice-chart 0.30 起 `virtual` 对**自定义系列**
  也成立 —— 走「惰性原始点」：**不建每根一个 `DataPoint`**（10 万根省 ~6MB、100 万根省 ~60MB），
  原始数据仍按引用保留（提示框 `params.data`、本包自己的字段解析都照旧），**像素缓存也照旧**。
  两条纪律：① 读点一律走 `series.pointCount` / `series.pointAt(i)` /
     `xValueAt(i)`，**不许直读 `series.points`**（列存下它是空的 —— 抬头读数踩过）；
  ② `renderAs: 'line'/'area'` **不加**这个默认：那会变成引擎的内置数值列系列，
     而类目轴（时间字符串）不吃那条路（会显式报错）。
- **列存的数据来源是「系列」，不是 `option.data`**（`candleColumnsFor(series, option)`）：
  环形形态下原始数据归存储所有、`option.data` 被摘掉，照它建列会得到**空列** ——
  蜡烛**静默不画**，而画布上还有坐标轴，冒烟测试照样绿（实测踩到：`validCount === 0`，
  画布只剩轴）。取数一律走公开访问器 `series.pointAt(i).raw`。
  缓存按**存储对象**（或数据数组）做键，并做增量维护：环滑动按 `raw.start` 平移几格
  （`copyWithin` + 只解析新进来的），尾部追加只解析新增的 —— 于是「追加一根」是 O(1)，
  不是每次重扫整个窗口。类目索引表存的是**相对偏移**（`shiftOffset`），滑动不必重写整张 Map。
  ⚠️ **复用判定不许带 `series` 身份**：归一化产物每次都是新对象，带上它缓存永远判不中
  （每根重建 10 万行 = 13ms/次，实测账见下节「大窗口实时流」）。
  ⚠️ 增量之后极值是陈旧的（滑动可能淘汰掉极值那根）：要精确极值先调 `ensureCandleRange()`。
- **大窗口的实时滚动走环形缓冲**（`appendData(id, [row], { maxPoints })`）：
  窗口满了覆盖最老的一根，**不 concat、不重建数据点**。两条配套：
  ① 环形形态下 `series.data` 会从 option 里摘掉（原始数据归存储所有）→
     价格轴量程要由应用给：`createTradingChart(target, option, { priceRange })`，
     运行中调整用公开的 `setDomain('y', [min, max])`；
  ② 每 tick 的成本分两段，**2026-09-23 在真实示例页上终测过**（`examples/streaming-candles.html?window=N`，
     页内自带计时：`#stat-tick` = `appendData`、`#stat-view` = `setDomainFromFractions` 那一趟）：

     | 窗口 | 数据路径 `appendData` | 视窗推进（流水线） |
     |---|---|---|
     | 1 万根 | 0.5 ms | 0.6 ms |
     | 5 万根 | 0.9 ms | 0.1 ms |
     | 10 万根 | **1.8 ms** | 0.2 ms |

     ⚠️ **更正（2026-09-24）**：`appendData` 这条随窗口线性增长（10 万 ≈ 1.4ms、20 万 ≈ 3ms），
     但**不是本包列的维护** —— 列存维护发生在渲染帧、且已修好（0.13ms/根，
     详见下节「大窗口实时流：列存复用」）；剩下的量在**上游**：滑动窗口下每次追加都要
     `refreshRawDomains` 重扫一遍（`readGenericPointInto` + `indices`）。要动得先跟用户确认。

实测见 `examples/streaming-candles.html`（页内自带环形 / 全量 setOption 的 A/B，
`?window=N` 调窗口；`#stat-frame` 是帧间隔中位数，**大窗口下看它最直观**）。

**上游瓶颈已修（ice-chart 0.29.1）**：`Axis.tickEntries()` 原来为**每一个类目**调
`scale.map()` + `formatTick()`，而类目轴的 `map` 内部是线性查类目 —— 10 万个类目时 `axisX`
一次渲染 **3716ms**（K 线组件同期只要 15ms，也就是说这 3.7s 全在上游的坐标轴上）。
0.29.1 修了两处通用问题：抽稀掉的刻度（`labels[i] === ''`）不进表 + `BandScale` 的
类目查表缓存 → `axisX` **3716ms → 0ms**、首帧 **3737ms → 17ms**（10 万根 K 线实测）。
### 依赖口径（要求 / 推荐两档，2026-09-23 定）

**本包自己不含任何图表架构**——列存 / 惰性原始点 / 环形缓冲全是 `ice-chart` 的能力，
本仓只做装配（**已经装配好了**，见下表；跟版号不是"启用架构"的开关）。所以依赖分两档写：

| 档 | 版本 | 含义 |
|---|---|---|
| **要求**（写进 `peerDependencies`） | `@damoqiongqiu/ice-chart` **≥ `^0.30.1`** | **架构完整**的第一版：列存（虚拟）系列 + 惰性原始点（自定义系列也能开列存）+ 环形缓冲。低于它，K 线会退回「每根一个 `DataPoint`」的老路；`0.29.1` 起还带类目轴 O(n²) 的修复。 |
| **推荐**（写进 README，不进 peer） | **≥ `0.30.4`** | 0.30.1 之上全是**性能与外观**改进（流水线增量：类目域/索引表、稠密轴标签；两处裁剪：高亮环、直角坐标系列）。**不影响正确性**，只是更快更干净 —— 所以不抬 peer 下限，避免逼所有应用为"零功能收益"升级。 |

`devDependencies` 跟到最新（现在 `^0.30.4`），让本仓的回归与示例跑最新内核；
npm 新装按 caret 也会解析到最新的 `0.30.x`。**别把 peer 退回 0.30 以下**，那会把上面那两件一起丢掉。

### 本仓用到的上游架构件（以及故意不用的）

| 架构件 | 怎么用的 | 位置 |
|---|---|---|
| 惰性原始点（`virtual`） | K 线**默认开**：`virtual: series.virtual !== false`（用户显式 `false` 才不覆盖） | `src/chart.ts` |
| 同上 | 派生的量柱也 `virtual: true` | `src/volume.ts` |
| 环形缓冲 | 实时追加走 `appendData(..., { maxPoints })` | 示例页 + 本仓测试 |
| 列存增量维护 | 环滑动按 `copyWithin` 平移 + 只解析新进来的 + 类目索引存**相对偏移** | `src/series/candleColumns.ts` |
| 组件裁剪 | K 线自己声明 `clipToBox = true` | `src/series/CandlestickSeries.ts` |

**故意不用**（不是缺口，是场景不需要）：**分块加载**与**引擎虚拟子源** —— 那两个是给
「亿级静态数据按需加载」的，实时 K 线窗口是滚动覆盖，用不上。

## 大窗口实时流：列存复用（**已修**；含一次被计时点误导的结论）

### 2026-09-23 第一轮：诊断对了一半

profile + 插桩（`streaming-candles.html?window=100000`）：每推一根调 2 次 `candleColumnsFor`，
其中**每根全量重建一次**（173 次 / 3 秒 = 1730 万行）；`shiftColumns` 环滑动**从未运行**；
复用被拒的原因 **100% 是 `cached.series !== series`**（其余判定全相符）。
`series` 是 ice-chart 归一化产物、每次 `applyOption` 都是新对象，而缓存真正的身份是
`raw` 存储（按 series id 复用、稳定）—— 所以那条身份比较让缓存**永远判不中**。

那一轮的错误结论是「**去掉身份比较更慢（2.8ms vs 1.7ms），所以增量路径比全量重建还贵**」。
这两个数量的不是一件事：1.7ms 是 `appendData` **那一次调用**（上游存储 + `refreshRawDomains`），
2.8ms 是**整帧** —— 而列存重建根本不在 `appendData` 里（它在渲染帧），所以那次 A/B
拿「上游成本」比「整帧成本」，方向被带偏了。

### 2026-09-24 复测：把计时点钉在真正的函数上

给 `candleColumnsFor` 进出各记一次时间 + 三条路径加计数器，跑真实页面 3 秒：

| 计数（3 秒 / 10 万根窗口） | 修之前 | 修之后 |
|---|---|---|
| `candleColumnsFor` 调用 | 360（每推 2 次） | 360 |
| **全量重建** | **180 次 × 10 万行** | **0** |
| `buildColumnsFromSeries` 累计耗时 | **2350ms / 3000ms** | **0** |
| 环滑动 | 0 次 | 180 次（每次 1 格） |
| 本包列存维护累计耗时 | **2350.7ms** | **24.7ms** |

**真正贵的是「每根重建整个窗口」（13ms/次），不是 `shiftColumns`** —— 环滑动一格只要
**0.13ms**（5 个 10 万元数组 `copyWithin` + 1 根解析 + 索引表增删）。前面那个「增量更贵」
的结论是被计时点误导的，**别再照它走**。

### 改了什么（都在 `src/series/candleColumns.ts`）

1. **复用判定不带 `series` 身份**：缓存身份是**存储**（`series.raw` / 数据数组），
   「能不能复用」由存储自己的状态判（容量 / 起点 / 长度 / 首末类目）。
2. **O(1) 复查尾根**（`refreshTailRow`）：环没滑动、长度也没变时，唯一还可能变的是
   **正在形成的那一根**（同一根反复推 → 原地改值）—— 复查一根、变了就地改写。
   于是「没滑动」不再等于「重建整个窗口」，正确性也没拿去做交换。

### 效果（同一台机器、同一页面，帧间隔中位数）

| 窗口 | 修之前 | 修之后 |
|---|---|---|
| 10 万根 | 60fps，但每帧里塞着 13ms 的列存维护 | 60fps（维护 0.13ms/根） |
| 30 万根 | **20fps**（50ms/帧） | **60fps**（16.7ms/帧） |
| 60 万根 | **10fps**（100ms/帧） | **60fps**（16.7ms/帧） |

用例：`tests/candle-streaming.test.ts` 三条新的钉住「增量 = 全量重建（滑动 + 追加 + 尾根改值）」、
「series 身份换了照样复用」、「同一根原地改值只复查尾部」。

**还剩什么**：`appendData` 自己仍随窗口线性增长（10 万 ≈ 1.4ms、20 万 ≈ 3ms）—— 那是**上游**
存储的事（滑动窗口下每次追加都要 `refreshRawDomains` 重扫一遍，`readGenericPointInto` + `indices`）。
要动得先跟用户确认（铁律 2）。**把 `open/close/low/high/valid` 做成环形布局不再是本包的方向**：
环滑动已经只要 0.13ms，剩下的是上游那一趟。

## 用浏览器验证前必须先 build

示例页加载的是 `../dist/index.umd.js`（构建产物），而 jest 走 `src`。
所以**改了 `src` 只跑 `npm test` 是假的绿灯**：浏览器里跑的还是旧包。
真实链路验证的顺序是 `npm run build`（或 `npm run verify`，它含 build）→ 起服务 → 看页面。
本项目已经因此误判过一次（`tooltip.trigger` 的默认值改了但页面没生效）。

## 图表外壳由页面画（HTML 覆盖层）

最新价线 / 右轴价签 / 左上角抬头 / 十字光标的两侧标签都走**页面 DOM**，不画进 canvas：

- 读数用 `createOhlcReadout()`（引擎没有十字光标位置事件，它包了 `controller.hover` 与
  `item:hover`/`item:leave`/`data:change`）；
- 定位一律用 `src/project.ts` 的 `plotRect` / `priceToY` / `yToPrice` / `categoryToX`，
  **不要用 `seriesComponent.pixelAt()`**（动画期间返回补间位置，实测踩过）；
- 引擎自带的十字光标 y 标签固定在绘图区**左**边缘，价格轴在右时必须
  `crosshair.showAxisLabel: false`，否则左边缘会多出一个价格标签；
- `tooltip.show: false` **只关浮动提示框**，不影响悬停与十字光标（`trigger` 默认 `axis`，
  即整列读数，抬头跟随光标靠它）；
- 重排按**几何签名**增量写 DOM，别每帧无条件写（`a11y-mirror.html` 就是这个套路）；
- **覆盖层挂在 `#panes`（整摞 pane 的容器）上，不是主图那一格**：准星的横线要画在指针
  所在的那一块里，盖不到下面就做不到。坐标原点要对齐到**主图画布左上角**
  （`syncChromeBox()`）—— `#panes` 有内边距，直接 `inset: 0` 会整体偏掉 (6, 4) px。

## 十字准星：引擎画竖线，页面画横线与两侧 chip

分工（照主流看盘软件的样子）：

**两条线都由页面画、都自由跟随指针**，引擎自带的准星整体关掉（`crosshair.show: false`）。

| 部件 | 谁画 | 位置 |
|---|---|---|
| 竖线 | 页面（`.vline`） | 指针的 x 上，从最上面那块绘图区的顶一直画到最后一块的底 —— 一条线就是「时间标尺」 |
| 横线 | 页面（`.hline`） | 指针的 y 上，但只画在**指针所在的那一块**里；读数用那一块自己的 y 轴（成交量 pane 上读出来就是成交量） |
| 右轴价签 | 页面（`.tag.price`） | 贴着绘图区右缘，纵坐标跟指针 |
| 时间签 | 页面（`.tag.time`） | **最下面那块**的时间轴上，跟着指针 x 走、文字取**最近那一根**；按标签实际宽度居中并夹在绘图区内 |

要点：

- ⚠️ **别把线交给引擎画**（2026-09-21 修，别改回去）。引擎的准星是**吸附到柱心**的：
  实测慢速横移 61 步（每步 1px）时，目标位置只变了 9 个值、竖线 **60 步里 53 步位移为 0**，
  剩下 7 步一次跳 8px —— 用户的原话就是「十字线感觉卡卡的」。
  横线本来就是页面画的、1:1 跟手，两者一对比更明显。改成页面画之后：位移步长恒为 1px、
  滞后 0px。**「卡」不一定是性能问题**：当时帧间隔 p50 16.7 / p95 17.6、一帧没掉，
  每步 JS 只有 0.2ms —— 纯粹是量化步进的观感问题，别一头扎进去优化渲染。
- 页面画竖线的前提是三块 pane 的绘图区 **x 范围完全相同**（`plot.x` / `plot.width` 一致，
  由 pane 栈的等宽对齐保证），所以一个 x 就能贯穿整摞图。
- 竖线只在 x 落在绘图区内时显示（横向上不越界）；纵向上**不看** `pointer.y`，
  这样跨 pane 分隔条时线不会断。
- 两条线都用同一套 CSS 虚线边框，节奏天然一致 —— 之前竖线是 canvas 的
  `setLineDash([3,3])`、横线是 CSS 边框，两者的虚线节奏其实不一样。
- 位置写 `left`/`top` 前要**对齐到设备像素**（`snapDevice`），否则 dpr=2 上竖线会发虚。
- 引擎的悬停状态照旧保留（`interaction.hover` 不能关）：抬头读数、时间签的文字都还靠它。
- 指针跟踪监听挂在**整摞 pane 的容器**上（`bindPointer`），不是某一块画布：
  否则副图上的横线没法画，而且 pane 之间那 1px 分隔条会让准星闪断。
- 悬停**不给任何系列加标记**，两处都要管住：
  1. `interaction.hover.mark: false` —— 关掉引擎那层。默认是**整列每个系列各画一个**
     （折线是半透明白填充的圆环，柱形是描边），一根 K 会连带 MA / BOLL 一起被圈上，
     白花花地盖住 K 线，比准星本身还抢眼；
  2. 蜡烛系列自己也**不画**悬停标记（`CandlestickSeries` 里已删，别再恢复）。
     它原本会在蜡烛实体上叠一圈白描边，把正要看的那根 K 盖住。

  判断标准很简单：**「读的是哪一根」由准星回答**，任何叠在 K 线上的额外装饰都是在
  妨碍读数。
- 准星价签与**最新价签**会叠住，阈值取标签的**实际高度**（`tagHeight`，首次绘制量一次）
  而不是拍一个数，否则会看到两个价签压在一起。

⚠️ 跨 pane 的准星依赖**上游两个修复**（2026-09-21 修，`ice-chart`）：
`ChartLink` 只认源头图发的 `item:leave`，以及 `InteractionController` 的
`externalHover`（指针只收自己放上去的悬停）。没有这两个修复时，被联动回显出悬停的副图
会在**同一次 mousemove** 里把回显删掉 —— 竖线永远只在指针所在的那一块出现。

## 拖动平移：要有「可拖的余量」，还要有「跟盘 / 手动」两态

**第一件事是数据量。** 平移被夹在**已载入的数据范围**内。如果「显示多少就载多少」，
默认缩放下拖动是零位移（`clamp(0 + shift, 0, 0)`，实测过）。所以图上载入
`count × HISTORY_FACTOR(3)` 根，默认只显示最后 `count` 根。

**而且左边必须是无限的（「无限画布」）。** 只载固定几屏的话，用户拖到底就撞墙 ——
他的原话是「拖到左侧边缘就无法再拖动了」。做法是三件套：

| 机制 | 作用 |
|---|---|
| `ensureHistory()` | 视窗左边缘离数据左边缘 < `HISTORY_PREFETCH`(400) 根就往前补 `HISTORY_CHUNK`(400) 根；**必须在拖动过程中补**（`scheduleViewRefresh` 里刷新之前），补晚一步就已经撞墙了 |
| `prependHistory()` | 从现有最老一根的**开盘价**往回接（新那根的收盘 = 右邻的开盘），价格连续、指标接缝不跳空；**不能碰 `this.price`**（那是现价） |
| `trimHistory()` | 全量历史的内存封顶（`HISTORY_MAX` 20000 根，只关内存）；裁的时候**不许裁进视窗左侧** |

**关键是「内存里的历史」和「交给图表的数据」要分开**（`renderSlice()`）：
归一化 / 指标 / 图元全是 O(n)，实测一次 `refresh` 在 360 根时 1.8ms、1560 根 6.5ms、
2760 根 10.7ms —— 拖动时每帧一次就顶到 60fps 预算。所以内存里存两万根，
交给图表的只有**视窗 ± `RENDER_MARGIN`** 那几百根，开销恒定（实测载入 1961 根时仍 1.9ms）。
`visibleRange()` 是这套东西的底座，**必须缓存**（见下）。

**第二件事是窗口怎么钉。** 别用 `dataZoom.start/end`（百分比）去对齐「第几根」：
`floor(66.666…% × 360)` 会落到 239 而不是 240，窗口变成 121 根；根数一变奇数，
刻度步长从 8 变 9，**末端两个时间标签就挤在一起**（实测踩到）。用 `pinToLatest()`
直接给两端的类目 key。

**第三件事是跟盘状态。** 窗口一旦锚在「左端某一根」上，那根要 240 根之后才会被移出数据，
于是新 K 会一直掉在窗口右边外面 —— 所以「不自动向右延伸」**不能是默认**：

| 状态 | 行为 |
|---|---|
| `follow = true`（默认） | 每次数据更新把窗口钉到最后 `count` 根，新 K 一直在右端 |
| 用户**动了时间窗口**（横向拖 / 时间轴滚轮缩放） | `follow = false`，视窗原样保留（不再向右延伸），并亮出「回到最新」 |
| 只动**数值轴**（纵向拖 / 标尺滚轮缩放） | **不算动过视窗**：跟盘保持、窗口继续跟着最新一根走，同时价格轴转为「手动量程」 |
| 按「回到最新」/「重置」/ 切周期 | 回到 `follow = true` |

⚠️ 中间两行曾经混在一起（引擎只要窗口动了就抛 `pan:change` / `zoom:change`，页面照单全收），
于是「只是把价格轴拉大一点」也会把跟盘踢掉 —— 时间窗口就停在原处、新 K 掉在视窗外。
现在的判据是 `viewKey`（视窗指纹 = 根数 + 两端 key）：只有指纹真的变了才算「用户动过视窗」，
页面自己摆窗口的地方（`pinToLatest` / `invalidate`）负责把它刷新。顺带这也把「一次联动
三个 pane 各抛一次事件」去重成一次重算。回归网：`e2e/terminal-pan.spec.ts` 的
「只动数值轴不算「动过视窗」」。

⚠️ 「回到最新」要**分两步刷新**：先 `refresh({preserveView:false})` 让图表拿到「最右端那段数据」，
再 `pinToLatest()` 钉窗口，最后 `refresh({preserveView:true})` 把渲染窗口收回来。
少第一步不行：目标 key 不在图表当前那段数据里时，引擎的 `clampAxisDomain` 找不到 key、
会把窗口解析成「整段数据」，视窗直接飞进老历史里（实测踩到）。
另外 `renderSlice()` 在跟盘时要**一定带上最右端**（`hi = total`），也是为这一步铺路。

**光标归页面**（引擎从不碰 `cursor`）：**按住主键**才是「抓住」的小手，松手（哪怕指针
已经拖到图外）立刻收回，悬停是普通箭头。两条都别改回去：悬停**不给** `grab`
（用户明确要求「不要一开始就把鼠标变成小手，要等用户按下鼠标之后」），
也别等到移动超过 3px 才变手 —— 那 3px 死区是引擎自己判断"算不算拖动"用的，
光标跟着它走的话，按住拖 1~2px 时用户完全看不到反馈。画线工具武装时这一片归画线层。
回归网：`e2e/terminal-pan.spec.ts` 的「光标只在按住主键时变成小手」。

## 右边也要是无限的（「未来空位」）

左侧无限靠补历史（上一节）。**右侧同理**：视窗停在最新 K 上时用户还是要能继续向左拖
（原话「当到达最右侧的最新数据时，依然需要能向左继续拖」），拖过去看到的就是一片空白 ——
未来还没有数据。做法是给池子尾部挂一串**占位类目**：

| 机制 | 作用 |
|---|---|
| `futureSlots` | 最新 K 之后的空位，`{ at, x }` 只有时间、没有 OHLC —— K 线 / 量柱读不出值就什么都不画，天然是空白 |
| `ensureFutureSlots()` | 保住尾部 `FUTURE_SLOTS`(600) 个槽。**槽锚在绝对时刻上**：新 K 进来时把已经变成真实 K 的从队首摘掉、队尾补齐，**已存在的 key 不变** —— 否则用户停在未来时，每来一根新 K 整个视窗都被顶一下 |
| `extendFutureSlots()` | 拖深了再补 `HISTORY_CHUNK`(400)，上限 `FUTURE_MAX`(2400) |
| `poolSlice()` / `poolIndexOf()` | 池子 = 真实 K + 空位，**视窗下标一律按池子算**：视窗落在空位上时下标比真实数据长度还大，不能再拿 `this.candles` 当全集 |

⚠️ 三条配套纪律，缺一条画面就不对（都实测过）：

1. **渲染窗口（`renderSlice`）必须带上最后 `count` 根真实 K**。视窗拖进空位深处后，
   交给图表的那一段可能**一根真实 K 都没有**，量程就没了依据 —— 价格轴掉到
   `[-148, 451]`、量图掉到 `[0, 1]`、副图掉到 `[-1, 1]`（MACD 的收盘价序列退化成一串 0）。
   只是把真实 K 带进**数据**里（视窗之外那些类目不会画出来），量程就稳住了。
2. **指标只喂真实 K**（`indicatorSource`）。指标输入是收盘价序列，空位没有 OHLC 时
   `closeSeries` 会**沿用上一根收盘**，均线 / MACD 于是在空位里拉出一条平线 ——
   看着像「指标跑到未来去了」。
3. **副图还要补一个「只带 x、什么都不画」的占位系列**（`blankAnchor`）。第 2 条让副图的
   指标序列止于最后一根真实 K，但类目轴是按「**这一格系列里出现过的 x**」建域的 ——
   副图里没有未来那串类目，联动过来的窗口在它那儿找不到右端 key，引擎只能让它停在老窗口上，
   于是「K 线已经是空白区、副图还在画老数据」，三块 pane 当场错位（实测：主图
   `16:31 → 02:26`，副图还停在 `03:06 → …`）。用 `type: 'bar'` + `value: null` 补齐类目
   （量图处理空位用的就是这一手，实测像素为 0）。

引擎侧还有一条耦合：类目轴的窗口端点越出数据时**只许「贴边滑」，不许放大成整段**
（见 `ice-chart/AGENTS.md`），否则副图那一格会被拉成整幅。

回归网：`e2e/terminal-pan.spec.ts`。

## 缩放的上下限与「缩小时不长出未来空位」

**限制交给引擎**（`interaction.zoom.minBarSpacing` / `maxBarSpacing`，px/根 口径，见
`ice-chart/AGENTS.md`）：示例页用默认值 —— 最密 0.5px/根、最粗半幅一根。
别自己按「占数据域的百分之几」算：那个比例会随「图上载入了多少根」漂移，实测缩到底到过
**0.24px/根**（一根占不到一个像素，整片糊成色带），放大那头则卡在数据域 5%。

**但「未来空位」要和缩解放一起想**：滚轮缩放绕指针锚定，窗口往两边一起长 —— 停在最新一根上
缩小时，右半边会一路长进那 600 个空位，画面右半变成一片空白（实测缩到底：3565 根里 2400 根
是空位）。所以页面在 `zoom:change` 上还加了一条闸门（`anchorRightOnZoom`）：

| 规则 | 为什么 |
|---|---|
| `cap = max(本轮右端, 最新一根)`，右端超过 cap 就整窗左移（跨度不变） | 缩小时该看到**更多历史**，最新那根始终贴在最右边 |
| 闸门**只拦「比之前更靠右」**，`pan:change` 会刷新 cap | 用户可能自己拖进空位看未来，缩小时不该把他拽回去 |
| 不在 `pan:change` 上拦 | 往左拖进空位正是「未来空位」这个功能本身 |

顺带一个前提：这类「缩到底会不会看到一片空白」的判断，**必须真的数一数窗口落在哪**
（`poolIndexOf(右端)` 和 `candles.length` 比），不能只看根数 —— 根数一样时，右端可能全在
空位里。回归网：`e2e/terminal-pan.spec.ts` 的「缩放有上下限」那条（同时盯根数、px/根、
右端不越界、三块 pane 同窗、轴标签不是一条色带）。

## 量程要按**可见窗口**算，不是按全量

载入 3 倍历史后，价格轴 / 成交量 / MACD 若仍按全量算，K 线会整体变矮
（随机游走的跨度约 1.7 倍，K 线只剩 ~58% 高）。做法：

- `visibleCandles()` 按当前 x 窗口切出可见段（图还没建好时退回「最后 count 根」）；
- ⚠️ 下标一律走 **`visibleRange()` 并且吃它的缓存**：`displayLabel` 是 x 轴 formatter，
  一次渲染按刻度数要被调十几次，每次都做两趟 O(n) 的 `indexOf` —— 全量到两万根时
  一次渲染就是几十万次比较（`visibleCount()` 也走同一个缓存，它连数组都不用切）；
- 价格轴显式给 `yAxis.min/max`（`withPriceRange` 只填**没写**的字段，所以会给显式值让路），
  成交量轴的 `max`、MACD 的 `min/max` 同理；
- 订阅 `pan:change` / `zoom:change`，rAF 节流后重算 —— **三块 pane 都要订**，
  联动的回显走 `silent()`，被联动那两块不会自己抛事件。
  实测单帧重算 ≈ 2.7ms，60fps 够用。

⚠️ 纵向拖动（`pan.axes: 'xy'`）过之后，引擎把那个 y 窗口记进 `viewState.y`，
页面重算的可见窗口量程就不再覆盖它 —— 这就是「手动量程」的语义。

**标尺那一整条带子归页面**（`bindAxisGestures()`），三个手势照主流看盘软件排：

| 手势 | 作用 | 落地 |
| --- | --- | --- |
| 右侧数值标尺**按住上下拖** | 缩放该 pane 的数值轴（向上拖 = 放大） | `beginValueAxisScale(chart, y)` + `applyValueAxisScale(chart, scale, y)` |
| 右侧数值标尺**双击** | 该 pane 的数值轴自适应（清掉手动 y 窗口） | `resetAutoScale(chart)` |
| 右侧数值标尺**滚轮** | 缩放该 pane 的数值轴（指针指着的价格不动） | `zoomValueAxis(chart, { factor, anchorY })` |
| 底部**时间轴双击** | 重置时间轴：默认根数 + 回到最新（跟盘） | 页面直接调 `resumeFollow()` |

为什么必须归页面：引擎的手势**一律要求指针落在绘图区内**
（`InteractionController.beginDrag` / `handleWheel` 都先判 `isInsidePlot`），标尺上它什么都不做 ——
「滚轮在图上缩时间轴、在标尺上缩数值轴」这种分工只能应用层自己接。判据就是两条带子：
右侧标尺 = 绘图区右缘 → 画布右缘且纵向在绘图区内；时间轴 = 最下面那块绘图区下缘 → 画布下缘。
落在绘图区里的双击 / 滚轮都不是这些手势。

动手前先记住三条：

- 引擎里**没有**只清 y 的公开入口：`setOption(…, { preserveView: false })`（或 `resetZoom()`）
  会把 x / y / 各副轴的窗口一起清掉，所以本包封了 `resetAutoScale(chart)` ——
  清完之后**把 x 窗口按原样写回去**，「y 归自动、x 不动」才是「价格自适应」的语义；
- 别用 `setDomain('y', 当前自适应值)` 顶替它：那只是把「手动窗口」写成恰好等于自适应的值，
  窗口依旧被钉死（`tests/axisView.test.ts` 里「量程真的跟着 option 走」那条就是防这个）。
- 全程 `silent()`：量程调整不是用户动过视窗，不能把「退出跟盘」误触发（e2e 有兜底）。

`zoomValueAxis()` 的口径与引擎自己的连续轴缩放一致（`zoomDomain` 的连续轴分支）：
新跨度 = 当前跨度 / factor，夹在「完整数据域 × `minSpan` / `maxSpan`」之间（这两个值优先读
option 里的 `interaction.zoom`，默认 0.05 / 1），**锚点处的价格纹丝不动**；
夹取与「不许出完整数据域」交给 `setDomain('y', …, 'zoom')` —— 走 `'zoom'` 这个来源才和
引擎的手势缩放同一条路径（`'api'` 不做这层约束）。滚轮倍率的默认值也和引擎对齐（1.2）。

**拖拽缩放（`beginValueAxisScale` / `applyValueAxisScale`）走的是「快照」口径**，
和滚轮不是一套，别混：

- 系数 = **「按下点到绘图区下缘的距离」/「当前点到绘图区下缘的距离」**，两边各加
  `0.2 × 绘图区高度` 做阻尼（指针贴到下缘时距离趋于 0，没有这一项系数会发散），系数下限 0.1；
  **向上拖 = 放大**。所以同一段拖动距离在不同高度上按下的力度是不一样的 —— 这是有意的。
- 每一帧都拿**按下那一刻的窗口**重算（`scale.domain`），不在上一帧结果上叠加：
  拖回出发点就是原样（不累积误差），拖出去再拖回来不会「回不去」。
- 锚点是**按下那一刻窗口的中心**（缩放前后中线上的价格原地不动）。
- 指针回到出发点时要**显式还原快照**（`scale.applied`）：只是一次点击（没拖动）时什么都别做，
  因为 `setDomain(…, 'zoom')` 会把窗口夹回完整数据域，那会让一次点击也跳一下。
- 按下时 `setPointerCapture`：拖动全程指针会跑出标尺、甚至跑出整张图，
  不抓指针就会中途丢掉 move 事件。
- 光标：标尺是**唯一悬停就给非默认光标**的地方（`ns-resize`）；标尺上的按下**不给平移小手**
  （`bindPanCursor` 里让路），否则「按住想缩放」会先看到一只抓住的手。

**刻度标签的精度是引擎按步长自适应的**（`formatNumberTick(value, tickStep)`：步长 < 1 时
`digits = ceil(-log10(step))`）。所以「标尺精度跟着缩放走」是免费的 —— 但要留意两点：

- 步长由 `niceStep(窗口跨度, tickCount)` 决定，**价格量级大的品种缩到底也可能一直是整数**
  （42000 附近的最小窗口约 40，步长 10）；小价格的品种才看得到小数位。
- 标签一长就可能**顶破 `axisLabelChars` 的补位预算**，那三块 pane 的右轴预留宽度就不同了、
  绘图区当场错位（AGENTS 上面那条「字号预算要够长」）。示例页的 e2e
  「标尺拖到很细：刻度跟着变细，三块 pane 的绘图区仍然等宽对齐」就是守这条。

**数值轴一动，外壳就要跟着重画**（`syncChrome(true)`）。两条容易被新手势带出来的毛病，
都在 `paintLastPrice` / `paintPointer` 里兜住了：

- 最新价被推出视野（纵向拖得远、标尺上缩得很小都会发生）时：那条虚线**不画**
  —— 它挂在整个 pane 栈的覆盖层上，画出去就落到下面那两块图上了；价签**钉在价格轴的
  上/下沿**，用户还知道最新价在视野的哪一侧。
- 指针停在右侧标尺上时**不出准星横线**：横线的读数只在「指针落在这一块的绘图区里」才有意义
  （此前只判了纵向，于是悬停标尺会画出一条悬空的横线 + 价签）。

## i18n：库只负责「库渲染的文案」，应用负责自己的

**库自己会渲染东西**（提示框四价、盘口表头、指标序列名），所以「文案」必须能被注入 ——
但不能把应用界面上的字也塞进库。分界线：

| 谁渲染 | 文案从哪来 |
| --- | --- |
| **库**：OHLC 提示框、盘口表头、指标序列名（图例可见） | `TerminalMessages`（`src/messages.ts`）：`ZH_TERMINAL_MESSAGES` / `EN_TERMINAL_MESSAGES` 预设 + `resolveTerminalMessages('en' \| 片段)`；入口是 `createTradingChart(…, { messages })` / `createOrderBook({ messages })` / `createOverlaySeries(…, { messages })` / `createMacdPaneOption(…, { messages })` |
| **数字与日期** | `TerminalNumberFormat`：`createTradingChart(…, { numberFormat: createIntlNumberFormat('de-DE') })`（价格轴刻度 + 提示框），成交量另给 `volumeFormat`；日期是坐标轴 `formatter` 的事（应用自己的本地化函数） |
| **应用界面**（抬头、面板、表单、表格、提示行） | 应用自己的目录 —— 示例页演示了姿势：`PAGE_MESSAGES.zh/en` + `data-i18n` / `data-i18n-title` 属性 + JS 里动态拼的文案走 `t(key, params)`（`applyI18n()` 一次性刷） |

三条纪律：

- **目录分组深合并**：只覆盖 `tooltip.open` 不会把同组其余字段清空（`resolveTerminalMessages`）。
- **显式参数优先**：`labels`（盘口列名）/ `priceLabels`（提示框行名）仍然压过目录 —— 需要「目录 + 单位」
  这种组合时（示例页的 `价格 (USDT)`）就用显式参数，换语言时再重算一遍（`book.setMessages(messages, labels)`）。
- ⚠️ **换语言不是「重建整摞 pane」**：文案与数字格式是 option 的一部分，改完 `invalidate()` 让 option
  重新求值即可；盘口用 `book.setMessages(...)`（只改表头文字，不重建结构）。

示例页「显示」面板里可切**中文 / English**，一次把三处都换掉（库的表头与抬头行名、页面的静态标签与
动态文案、以及周期显示名）。演示页**没有**做 i18n 的部分是：下拉面板里的选项文字与 `setHint()`
的动态提示（那是应用自己的业务文案，接法与静态标签完全一样）。

回归网：`tests/messages.test.ts`（目录 / 合并 / 数字格式 / 提示框 / 价格轴 / 盘口）、e2e「i18n」。

## 时间标签：类目 key 要「稳」，显示要「跟窗口变」

类目轴按字符串去重，**key 必须全局唯一**。载入 3 倍历史后跨度会跨天，所以

- `keyOf()` 产出的 key **一律带日期**（盘中是 `YY-MM-DD HH:MM`），只负责唯一性；
- `displayLabel()` 按**当前可见跨度**决定露多少：一屏不足一天只露 `HH:MM`（取 key 后 5 位），
  跨天才把日期露出来；日线同理（不足一年多只给 `MM-DD`）。

两者不能混在一个字符串里：key 要稳定（窗口一变就换 key，视窗锚点会错乱），
显示要跟着窗口变。轴刻度 formatter、抬头的时间、准星的时间签**三处都走 `displayLabel()`**。

## 副图（pane 栈）的两条纪律

1. **更新必须走 `stack.refresh()`**：`setOption` 是**整体替换**（`ICEChart.applyOption` 里
   直接 `this.option = option`），应用层拿原始 option 调会把 `createPaneStack` 注入的
   「等宽主题 + 定宽刻度标签」一起冲掉 —— 冲掉之后三块绘图区的右边缘立刻错位。
   这也是 `PaneSpec.option` 支持传函数（每次 refresh 重新求值）的原因。
2. **横向对齐靠两件事**：等宽字体（`PANE_FONT_FAMILY`）+ 刻度标签补到固定字符数
   （`fixedWidthAxisFormatter`）。右轴预留宽度 = 最宽标签 + 固定间距，所以标签字符数一致
   才能让各 pane 的绘图区等宽。**字号预算（`axisLabelChars`）要够长**：标签超过预算就不补了，
   对齐随即失效（实测 `42100.5` 是 7 字符，8 才安全）。

## 数值轴的刻度密度（别让它稀下去）

引擎的默认档数是**固定 5 档**（`buildAxisLayout` 里 `axisOption.tickCount || 5`），
287px 高的价格轴上一档就是 57~71px —— 用户的原话是「K 线右边标尺上的数字那么稀疏」。
主流看盘软件的口径是按像素给密度：**一档留出 `字号 × 2.5` 的高度**（字号 12 → 30px）。

`axisTickCount(min, max, length, { spacing, nice })`（`src/panes.ts`）把这件事做实，
pane 栈在 `alignAxes` 里给**没写 `tickCount`** 的 y 轴补上。四条坑都在函数里写死了理由：

1. **不能直接传「想要几档」**：引擎 `niceStep` 把 `跨度 / 档数` **严格向上**取到
   1 / 2 / 5 × 10^k，而且**取两次**（先 `niceDomain` 取整数据域、再给刻度取整），
   两次叠起来会在跨 10^k 的地方直接稀一半。实测跨度 957 / 轴 287：档数 11 →
   域取整成 1100 → 再取整成 200（一档 57px）。所以要**模拟整条链路**再反查档数。
2. **拿「显示中」的窗口算，不是 option 里的 `min` / `max`**：手动量程（纵向拖 / 标尺缩放）
   之后两者不是一个东西 —— 拿后者算会得到「窗口已经缩到 275 宽、档数还按 501 宽给的 12 档」，
   一档当场 52px（实测）。`alignAxes` 收的 `views` 就是 `chart.getAxisDomain(i)`。
   视图域已经是引擎取整过的，这时 `nice` 传 `false`（只会再取整一次）。
3. **尺寸算完要重算一遍**：密度依赖绘图区高度，而高度只有排完版才知道。
   `createPaneStack` 的 `resize()` 里排完尺寸后**再应用一遍 option** —— 不重算的话
   首帧量到的是画布默认尺寸（600px 的容器被算成 ~91px，价格轴只剩 4 档），
   而且容器尺寸变化后也不会更新。
4. **梯子是 1/2/5，落点有粒度**：一档实际落在 `spacing / √2.5 ~ spacing × √2.5`
   （目标 30px 时是 19~47px）。这是引擎步长梯子决定的，别指望更均匀；
   想更密就调 `spacing`（示例页传的是主题字号 × 2.5）。

回归网：`tests/panes.test.ts`（`axisTickCount` 四条 + 建栈后数真实标签）、
`e2e/terminal-pan.spec.ts` 的「刻度密度」那条（默认档数 ≥ 8、一档 19~47px、
缩放后仍然稳、三块 pane 等宽）。

## 背景是「方格」：竖线靠 x 轴抽稀表对齐

示例页三块 pane 都开 `grid: { x: true }`（引擎默认只有横向平行线），竖线的位置由引擎给：
**跟 x 轴标签抽稀同一批**（`labels[i] === ''` 的那几颗不画）。所以：

- 竖线数量 = 时间标签数量，位置与标签严格一致 —— 这就是「方格」；
- 三块 pane 的 x 窗口相同、绘图区等宽、formatter 也相同 → 抽稀结果相同 → 竖线贯穿整摞 pane。
- 上面两块 pane 的 `xAxis.show: false`（省纵向空间），但**抽稀表照出**（只是不占排版空间、
  Axis 也不画）—— 没有这张表，网格只能退回 `scale.ticks()`，而类目轴的 `ticks()` 会返回
  整个 domain：视窗里 120 根就是 120 条竖线，一片栅栏（实测）。

⚠️ 这条依赖上游（`ice-chart`）的两个能力：① 抽稀对隐藏的 x 轴也做；② `GridLines` 用布局表的
抽稀结果而不是 `scale.ticks(5)`。上游的坑与回归网记在 `ice-chart/AGENTS.md` 的「x 轴标签抽稀
只有一处」那一节（`tests/chart/grid-x.test.ts`）。本仓的 e2e「背景是方格」在**未来空位**里扫
像素数竖线：数量 = 真画出来的标签数，且三块 pane 的竖线 x 一致（±1.5px）。

## 键盘：视图动作归页面，引擎的键盘要关掉

主流看盘软件的方向键是**视图动作**：`←` / `→` 平移时间轴、`↑` / `↓` 缩放数值轴、
`+` / `-`（`=` / `_` 同义）缩放时间轴、`Home` 回到最新。引擎自带的那套不是这个语义 ——
它把 `←/→` 当成「在可见数据点之间挪**活动点**」、`↑/↓` 换系列，而且会把悬停（准星 + 读数）
挪到数据点上，和「准星跟指针」直接冲突。所以示例页 `INTERACTION` 里 **`keyboard: false`**，
由 `bindKeyboard()` 接管。

两条纪律：

- **表单里不抢键**（`INPUT` / `TEXTAREA` / `SELECT` 直接放行），带修饰键（meta/ctrl/alt）也不抢 ——
  自定义周期、下单面板、浏览器快捷键都还要用。
- 视图动作一律走公开的 `panTimeAxis()` / `zoomTimeAxis()` / `zoomValueAxis()`（`src/axisView.ts`），
  于是和鼠标手势共用同一套引擎夹取（`minBarSpacing` / `minSpan`），并且照常抛
  `pan:change` / `zoom:change` —— 页面的「跟盘 → 手动」那条链、量程重算、渲染窗口收放
  全都不用另写一份。平移走 `source: 'pan'`（只要求与数据交叠 1/4 个窗口，不会把平移夹成缩放）。

回归网：`tests/axisView.test.ts` 的「时间轴的键盘视图控制」四条；e2e「键盘：←/→ 平移时间轴、
↑/↓ 缩放价格轴、+/- 缩放时间轴、Home 回到最新」。

## 每块 pane 左上角一行图例（+ 主图水印）

主流看盘软件每块 pane 的左上角都有一行：主图是「交易对 · 周期 + 开高低收量 + 涨跌」，
成交量是「Vol + 当前量」，副图是「MACD 12 26 9 + DIF/DEA/柱值」（值随涨跌上色）。
本仓把这三行都画在 **chrome 覆盖层**里（不是 canvas）：文字在 `renderReadout()` 里按签名增量写，
位置在 `placeTitles()` 里按各 pane 的绘图区几何贴（`syncChromeBox()` 末尾调用）。
主图水印（大号淡字）也在这一层，贴在价格绘图区正中。

⚠️ 图例读的是**最后一根真实 K**：视窗停在最新一根时，渲染窗口的尾巴上挂着几百个「未来空位」
（只有类目、没有 OHLC）。`createOhlcReadout()` 早先的兜底是「`points.length - 1`」，
拿到的正好是空位 → 读不出 OHLC → 返回 null → **没有悬停时抬头和图例就是空的**（实测踩到）。
现在 `read()` 的兜底会往前找到**最后一根读得出 OHLC 的**（按点数缓存，数据刷新不重扫）。
回归网：`tests/readout.test.ts` 的「尾巴上挂着未来空位时，兜底读最后一根真实 K」。

副图的指标值（DIF/DEA/柱、RSI）由页面按 `indicatorSource` 现算，按
「数据长度 + 周期 + 副图类型」缓存 —— 指针在图上移动会让 `renderReadout()` 跑很多次，
每次重算一遍 O(n) 的 EMA 是白花。

## 图表类型 / 价格轴坐标 / 指标参数（这一轮新增的三组「可配置」）

- **图表类型**（蜡烛 / 空心蜡烛 / 折线 / 面积）：`renderAs` 是**渲染类型**，与
  `type: 'candlestick'` 的**交易语义**分开 —— 影线量程、成交量副图、OHLC 抬头 / 提示框
  永远按 K 线算，切类型不丢任何读数（`toTradingOption` 里 `isCandle()` 认 `renderAs`，
  输出系列才换成 `line` / `area`）。回归网：`tests/option.test.ts` 的两条 + e2e「图表类型」。
- **价格轴坐标**（右键标尺）：自动量程 / 线性 / 对数 / 百分比。对数走引擎的
  `yAxis.type: 'log'`；**百分比只改标签口径**（以**可见窗口第一根**的收盘为基准），
  刻度位置仍是价格口径 —— 引擎的刻度是按数据域取整出来的，挪不到整数百分比上，
  这是当前能力的边界（AGENTS 里写清楚，别当成 bug 反复查）。
- **指标参数**：MACD 快/慢/信号、RSI 周期、MA / EMA 两条、BOLL 周期都在「指标」面板里改
  （`change` 事件，不是 `input` —— 每敲一个字符重建一次图表太浪费）。副图图例的值与
  抬头签名都带参数，改完立刻跟着走。

## 左侧画线图标条 + 会话分隔线 + 磁吸 + 均量线

- **左侧画线图标条**（`syncDrawBar()`）：主流看盘软件把画线工具放在图表左边缘、竖排图标；
  选中态高亮、`title` / `aria-label` 齐全、Esc 退出。结构放在 `.chart-body` 的左侧
  flex 子项里（42px），图表的几何全部靠 `getBoundingClientRect` 现算，所以加这一条不影响
  外壳对齐。顶部工具条里的「画线」下拉保留（两个入口都写同一个 `drawing` 状态）。
- **会话分隔线**（`syncSessionLines()`）：每天第一根上一条极淡的竖线，画在**外壳覆盖层**里
  —— 也就是画在画布上面（引擎没有「第二套竖线」的能力）。颜色取到 0.055，落在 K 线上几乎
  看不出；关掉时**要把 DOM 清干净**（留着虽然看不见，用例会数出旧线，实测踩到）。
- **磁吸**（`paintPointer` 里）：把横线与价签吸到这一根的**最近那个价**（开高低收四选一），
  横线的 y 用吸附价反算，不是指针的 y —— 否则线吸住了、标签还在指针上。
- **均量线**：量图上加 MA5 / MA10 两条线，数据跟量柱同一批 x；**首尾读不出量的点必须裁掉**
  （用 `seriesData()`）—— 折线系列会把 null 点当 0 画出一条假竖线（上游行为）。

## 主题：一套 token 驱动三处（`src/theme.ts`）

外观**只有一处来源**：`TerminalTheme`（[src/theme.ts]）。它同时喂给：

| 去哪 | 怎么接 | 说明 |
| --- | --- | --- |
| **图表** | `terminalThemeToChartTheme(theme)` → `createPaneStack({ theme })` | 得到 `Partial<ChartTheme>`，再经 **ice-chart 的主题桥**（`chartThemeToEnginePatch` → `applyChartThemeToEngine`）推到 **ice-render 的引擎主题** —— 所以引擎自己画的东西（画布底色、交互外壳、字体）也跟着走，**应用侧不需要自己调引擎的 `setTheme`** |
| **外围组件**（盘口） | `createOrderBook({ theme })` / `book.setTheme(theme)` | 面板 / 文字 / 悬停底写成组件根节点上的 CSS 变量（`--ice-book-*`），涨跌色取自 token 的 `up` / `down`（显式传 `upColor` / `downColor` 时以显式值为准） |
| **页面外壳** | `applyTerminalTheme(theme)` | 把 token 写成一组 CSS 变量（`--bg` / `--panel` / `--up` / `--accent` …，名字表 = `TERMINAL_THEME_VARS`），页面样式表一律 `var(--xxx)` |

预设：`DARK_TERMINAL_THEME`（默认）/ `LIGHT_TERMINAL_THEME`；`resolveTerminalTheme('light' | 'dark' | 片段)` 做解析。

⚠️ **主题是在建栈那一刻解析的**（等宽字体、字号、配色都从它来），之后 `refresh()` 注入的还是那一份 ——
所以换肤必须显式调 **`stack.setTheme(next)`**（它重新求值 option 再应用三块 pane）。
只改 `option.theme` 不会生效；重建整摞 pane 会连缩放窗口和画线一起丢。
盘口那边对应 `book.setTheme()`，页面外壳那边对应 `applyTerminalTheme()`（CSS 变量，天然不用重建）。

蜡烛的涨跌色**不在图表主题里** —— 那是系列 option（`candle.upColor` / `downColor`），
因为同一张图上不同系列可以有不同配色；页面从 token 的 `up` / `down` 取，于是换肤时蜡烛也跟着走。

回归网：`tests/theme.test.ts`（预设 / 映射 / 变量名契约）、`tests/panes.test.ts` 的 `setTheme` 那条、
`tests/orderBook.test.ts` 的主题那条、e2e「盘面主题」（CSS 变量 + 图表底色 + 盘口文字三处一起换，可换回）。

## 盘面观感与布局（对齐主流合约交易所的盘面）

示例页的排面按合约交易所那套来：**深色盘面 + 顶上一排行情 + 三栏 + 底部选项卡**。

| 区域 | 结构 |
| --- | --- |
| 顶部 | 行情跑马灯（一格一个交易对，涨跌上色）→ 交易对抬头（符号 + 标记价 + 涨跌 + 一排「标签/数值」：标记价 / 指数价 / 资金费率与倒计时 / 24h 高 / 低 / 量 / 额）|
| 三栏 | 图表（含左边缘画线图标条）· 订单簿 + 最新成交 · 下单 + 账户 |
| 底部 | 持仓 / 当前委托 / 画线记录 —— **选项卡**（选中项下方一条品牌黄），并且**横跨图表 + 盘口两栏**（和右侧下单栏同高）|

配色（`:root` 一套变量，图表主题也吃同一套）：底 `#0b0e11`、面板 `#181a20`、面板内层 `#1e2329`、
边框 `#2b3139`、正文 `#eaecef`、次要 `#b7bdc6`、弱化 `#848e9c`、强调（品牌黄）`#f0b90b`、
涨 `#0ecb81`、跌 `#f6465d`。**默认涨绿跌红**（合约盘面的通行口径），「显示」面板里可切红涨绿跌。

几个必须记住的点：

- **盘口两侧的颜色是「卖盘 = 跌色、买盘 = 涨色」**（`src/orderBook.ts`）：合约盘面上卖盘偏红、
  买盘偏绿，换了涨跌配色两边跟着翻。盘口有表头（价格 / 数量 / 合计）与**合计列**（从最优价往外累加），
  深度条画在合计列背后。
- **盘口按自身高度排，剩余高度给最新成交**：早先按 55%/45% 分 flex，十档排不下就画到成交那一格上
  （实测两块内容叠在一起）。`#order-book { flex: 0 0 auto }` + `.trades-wrap { flex: 1 1 auto }`。
- 报价一律**千分位 + 固定小数位**（`formatPrice(v, 2)` → `82,532.40`）。刻度标签的补位预算
  （`axisLabelChars`）要按最长标签给足（`42,300.00` 是 9 字符 → 示例页给 10）。
- e2e 的像素判据要**按绘图区矩形**扫（`chart.layout.plot`），别按画布内缩一个固定像素数：
  后者会把右侧的价格标尺扫进去，轴标签颜色一变就会把「空白区」判成有内容（实测踩到）。
- ⚠️ **下拉面板会盖住画布**：面板开着时指针落在面板上，`#panes` 收不到 pointermove ——
  e2e 里「先开面板勾选项、再悬停图表」必须**先按 Esc 收起面板**（实测踩到）。
- ⚠️ **外围条的高度必须稳定**：交易对抬头里那些数值（`29.17B` → `9.87B`）宽度会变，
  脚注的提示文案长短也会变 —— 只要它们让**任何一条外围条换行 / 变高**，图表区（flex 项）
  就跟着变，于是「同一段视窗、量两次几何竟然不一样」，像素类用例当场飘（实测踩到两次）。
  对策：数值给 `min-width` + `tabular-nums`，脚注固定高度、提示那句用 flex + 省略号。
- ⚠️ **图表高度不要写死**：`createPaneStack` 的 `height` 一旦写死（早先是 540），
  窗口一窄 / 换浏览器（字体换行不同 → 顶部那几条更高）图表就超出容器被裁掉，
  **最下面那块的时间轴跑到容器外**，双击时间轴收不到事件（实测在 Firefox 上踩到，
  Chromium 刚好没触发）。现在不传 `height`，交给 pane 栈读容器的 `clientHeight`，
  `#panes` 只留一个 `min-height`。

## 报价精度（小数位）与补位预算

`TradingChartExtras.pricePrecision` 给价格轴装一个**固定小数位**的 formatter
（`formatPrice(value, 2)` → `41800.00`，**不去尾随 0**：价格轴要的是一列对齐的报价），
提示框里的四个价也走同一个精度。不给就保持引擎原来的自适应写法（整数刻度写整数）。
示例页传 2，页面自己画的价签 / 抬头也走 `formatPrice(v, 2)`，四处口径一致。

三个必须记住的点：

- ⚠️ **`toTradingOption` 必须在 `alignAxes` 之前**。pane 栈的定宽 formatter 是**包在外层**的，
  先包一层空壳再让 `toTradingOption` 往轴上塞报价精度，它会看到「已经有 formatter」而让路 ——
  两位小数永远加不上（实测踩到，e2e「价格轴刻度带两位小数」守着）。
- **补位预算 `axisLabelChars` 要跟着加长**：`41800.00` 是 8 字符、六位数价格就是 9，
  预算不够就不再补空格，三块 pane 的右轴宽度立刻不一样（示例页从 8 提到 9，实测三块绘图区
  仍是 12 / 833）。
- `pricePrecision` 只动**第一根 y 轴**（价格轴）与提示框；用户自己写了 `yAxis.formatter` 时不覆盖。

## 类目轴取「最近类目」要用 invert 而不是 indexAt

`BandScale` 的类目带之间有 `paddingInner`（默认 0.2）的**空隙**，`indexAt` 落在空隙里返回 -1
（`indexAt` 只判「在带内」）。指针->类目一律用 `scale.invert()`（命中不到给最近类目），
`src/project.ts` 的 `xToCategoryIndex` 就是这个口径 —— 早期用 `indexAt` 时，画线时点在两个蜡烛
之间会「没反应」。

## 折线系列的 null 点会被当成 0（上游行为，已在应用侧规避）

ice-chart 归一化时把 `y === null` 的点写成 `top: 0`（`normalize.ts`），
`computeEffective` 只在 `top === null` 时给 NaN，于是这个点按 **0** 参与绘制 ——
折线会从第一个有效值处拉出一条竖直假线（实测像素 y 落在 `yScale.map(0)` 上，不是 NaN）。

规避：`seriesData()` 默认**裁掉首尾 null**（均线预热期正是这种情况）。
**中间的空洞目前没法在应用侧规避**，要等上游把 null 点真正断线。

## 示例页的成员顺序（棘轮会卡）

`static 字段 → static 方法 → 实例字段 → 构造函数 → 访问器 / 实例方法`。
最容易写错的是把 `static round2()` 这类工具方法顺手写在构造函数后面 —— 棘轮判成
`S*F*C*T*...` 直接失败（本项目连踩三次）。写新页面时先把 static 段落收齐再写字段。

## 高分屏必须显式传 dpr（引擎默认 1）

`ICE.dpr` 默认是 **1**，引擎**不会**自己去读 `window.devicePixelRatio`
（`ICEChartOptions.dpr` 存在，但不传就是 1）。不传的后果不是「稍微糊一点」：
画布的 backing store 只有 CSS 尺寸，浏览器把 1x 位图放大 2~3 倍显示 ——
轴线、刻度、K 线、文字**全都发虚**（全家族 30 个示例都没传，实测踩到）。

本包已经**默认补上**：`createTradingChart` / `createPaneStack` 不传 `dpr` 时取
`window.devicePixelRatio`（上限 3，见 `src/device.ts`），显式传 `dpr: 1` 可退回旧行为。

## `unit()` 是「设备像素」不是「CSS 像素」

基类 `ChartComponent.unit()` 的定义是「一个**设备**像素在当前 ctx 变换下的长度」
（`1/(vp.scale·dpr)`）。所以 `style.borderWidth * unit()` 得到的是 **borderWidth 个设备像素**：
dpr = 1 时看着正好，dpr = 3 时只剩 1/3 个 CSS 像素 —— 细得像头发丝。
要按 CSS 像素给线宽必须再乘 dpr，蜡烛里封成了 `cssUnit()`（= `unit() * dpr`）。

同理，填充类几何要对齐**设备像素边界**（`Math.round(v/unit())*unit()`），
而 1px 线要对齐**像素中心**（基类 `snap()` 已经做了）。

## 柱间距：实体要铺满类目带宽

类目轴已经用 `paddingInner`（0.2）把 **20% 的步距**留成间隙，所以带宽 = 0.8 × 步距。
如果实体再乘一个小比例（早先默认 0.66），实体就只占步距的 53% —— **一半都是缝**，
用户一眼就说「太稀疏」。

主流终端库的口径是「实体占步距的 82%~86%」（`optimalCandlestickWidth`：
`spacing × coeff`，coeff 从小间距的 ~0.86 渐近到 0.8，即间隙 14%~20%）。
我们这边的等价做法就是 **`barWidth` 默认 1（铺满带宽）**，间隙 = 20% 的步距，同一个量级。

要更细自己传 `barWidth`（≤1 是占**带宽**的比例，>1 是绝对 CSS 像素）。
成交量柱同理默认铺满 —— 比蜡烛窄会让副图看着比主图稀疏，两者对齐才好读。

## 蜡烛的水平对齐：影线与实体必须同轴

影线和实体**不能各自取整**：实体左右边缘四舍五入到整数设备像素、影线单独用 `snap()`
（它加了半个像素），两套取整合起来**根根蜡烛都歪 0.5 设备像素**（实测 meanAbs = maxAbs = 0.5），
用户一眼就看出来「影线没居中，很诡异」。

正确做法是从**同一个设备像素几何**推出来（`CandlestickSeries.candleGeometry()`，绘制与命中共用）：

1. 影线中轴按**宽度奇偶**落在正确的像素上：奇数设备像素取半像素（经典 1px 对齐），
   偶数取整数像素；
2. 实体宽取**与影线同奇偶**的设备像素数 —— 这样左右边缘落在整数像素上（填充清晰），
   且严格以影线为中轴。

回归测试在 `tests/device.test.ts`（dpr = 1 / 2 / 3 三档都断言偏差为 0）。

## 影线不能从实体中间穿过去

蜡烛的影线要画成**实体上下两段**（`high → 实体上沿`、`实体下沿 → low`），
不能一条线从 high 贯到 low：实心实体盖得住，**空心阳线会把中间那段露出来** ——
一条竖线穿过蜡烛正中，看起来像画错了（用户实测反馈）。

## 价格轴为什么要自己钉（最容易踩的坑）

`ice-chart` 的 `buildYDomain` 只收归一化后的 `y`（本包默认取收盘价字段），而
`min` / `max` 是**在极值之后覆盖**的，且**显式写了的那一侧不再施加自动留白**。
所以：

- 不钉 → 影线（low / high）落进绘图区之外，被裁掉；
- 钉了 → 留白要**我们自己给**（`computePriceRange` 的 `padding`，默认 6%），否则刻度贴着影线端点。

改了数据之后记得重钉：`chart.setData()` **不会**重算价格轴。正确做法是
`chart.setOption(toTradingOption(nextOption), { animate: false, preserveView: true })`
（示例页 `examples/candlestick.html` 就是这么写的）。

## 测试约定

- `npm test` 跑 jest（jsdom + `tests/setup/canvas-env.ts` 的 Canvas 2D 桩）。
  **不要 mock 引擎**：集成用例走真实渲染 → 真实命中测试，才能抓到「渲染与命中不一致」。
- 集成用例里**关掉动画**（`animation: { enabled: false }`）或**直接查比例尺**算坐标：
  入场动画期间 `pixelAt()` 给的是补间中的位置，拿它做命中断言会飘。
- 新增能力同步补三类用例：纯函数（`ohlc` / `axisRange`）、option 补齐（`option`）、
  端到端交互（`candlestick`）。
- **示例页的行为要有 e2e 兜底**（`e2e/terminal-pan.spec.ts`）：平移 / 未来空位 / 光标这些
  住在 HTML 里的逻辑，jest 照不到。判据一律量**行为**而不是量实现：光标值、视窗两端 key、
  三块 pane 是否同窗、绘图区里「系列颜色」的像素数（空白区必须是 0）。
- ⚠️ 仓库里的 playwright 只跑 chromium，但**用户的主力浏览器是 Firefox**
  （手势、光标、指针捕获这些最吃浏览器差异）。动手改鼠标手势时建议再跑一遍 Firefox：
  `npx playwright install firefox` 之后用一份临时 config（`projects: [{ name:'firefox',
  use:{ browserName:'firefox' } }]`，其余继承 `playwright.config.ts`）跑 `e2e/terminal-pan.spec.ts`，
  跑完把临时 config 删掉（别把第二个 project 提交进去，发版门禁要快）。

## 外围组件也归本包

交易专属的**非图表** UI 也放在本包（盘口 `src/orderBook.ts` 是第一个）—— 通用图表库不该认识盘口，
而本包的定位就是「交易语义的唯一落点」。约定：

- **自包含**：结构 + 样式（注入一次、id 守卫）+ 交互都在组件里，页面只给一个容器；
- **结构只建一次**，`update()` 只改数值；变更按**签名**去重，数据没变时一次 DOM 都不写
  （伪实时页面每帧都在推数据，无脑重建会掉帧）；
- **语义交给调用方**：组件不猜「最新价该跟谁比」，颜色用 `midColor` 传进来；
- 数据方向写进类型注释（盘口是「最优价在前」），页面就按契约给。

## 顶部工具条：库组件（三层定制）+ 占带

图表顶部工具条照主流终端的做法组织（用户明确要求「时间框架、指标这些放 K 线图顶部，
有些选项用下拉展开」）。**它现在是库里的组件**（`src/toolbar.ts` 的 `createChartToolbar`）：
页面只留业务判断（切周期要重订阅、改参数要重算指标、画线动作要动 `drawing`），
图标 / 主题 / 文案 / 菜单容器 / 收起时机 / 状态同步全在库里。设计稿与取舍见
`plans/chart-toolbar.md`。

- **常显一排**是「收藏」的周期，一键切换；完整列表在**周期下拉**里，按
  分钟 / 小时 / 天 / 周 / 月 / 自定义分组，每行右侧的 ★ 加入或移出收藏（收藏项会出现在那排上），
  顶部有「＋ 自定义周期」（类型 × 数值）。
- **指标下拉是复选**（主图叠加可多选：MA / EMA / BOLL），**副图是单选**（MACD / RSI）；
  勾选时下拉**保持展开**（连着勾几个不用反复点开），选周期 / 选画线工具 / 执行操作则自动收起。
- 同一时刻只开一个下拉；点空白处或 Esc 收起；面板内容归应用，行上的点击**委托在面板根上**
  （库那一层也拦住了面板内点击冒到 document —— 否则勾一个指标就把面板关了）。
- 收藏与自定义周期存 `localStorage`，刷新后还在。

三层定制（示例页用查询参数各跑一条：不写 = 完整定制、`simple`、`custom`、`replace`、`none`）：

| 层 | 入口 | 给谁用 |
|---|---|---|
| 1 · 声明式 | `items: ['intervals', 'type', 'display']` —— 数组顺序即显示顺序，不写就不显示 | 只想少放几项 |
| 2 · 自定义项 | 同一个数组里混 `{ id, label, icon, onClick(ctx), active(ctx), menu(ctx) }` | 想加自己的按钮 / 面板 |
| 3 · 整条替换 | `render(ctx) => { element, update }`，或 `show:false` | 想整条自己画 |

⚠️ 四条踩过的坑（都在示例页里守着）：

1. **占带走 `stack.setToolbar()`**，不是自己往页面里塞一行：带插在 pane 栈容器顶部，
   pane 高度从容器高度里扣掉它（悬浮会盖住最上面那几根 K 线）。**换带之后必须重新求值
   每格的 option**（`setToolbar` 内部走 `resize()`）—— 数值轴的档数是按「绘图区高度 ÷
   一档的最小像素」反推的，只重排尺寸不重算，一档当场挤到 17px（刻度密度用例当场红）。
2. **每帧只 `toolbar.update()`，不重填面板**：`update()` 按签名去重（没变时一次 DOM 都不写），
   重填面板会把用户正在输入的参数框冲掉。占带那条 `update` 回调因此传的是
   `() => toolbar.update()`，只在明确的动作里才重填面板。
3. **按钮上的字可以是函数**（`label: (ctx) => …`）：周期按钮上的 `5m` 就是这么来的；
   按钮的 `title` 也按签名更新（换语言时悬停提示必须跟着换）。
4. **自定义面板自己决定点完关不关**（`ctx.closeMenu()`）：指标多选要连着勾几个，
   选周期 / 选语言就该关掉 —— 库里不知道，所以给一个显式的口子。

⚠️ **自定义周期的条数要按「类目标签唯一」反推**：盘中周期不超过一天、日线以上不超过一年，
再夹到 20~120 档。`get interval()` 必须走 `allIntervals()`（内置 + 自定义），只查内置表
会在切到自定义周期时取到 `undefined`（实测踩过）。

## 指标预热期：靠「渲染窗口的余量」留出视野，别再裁数据

MACD(12,26,9) 要 **33 根**才有第一个柱值（慢线 26 + 信号线 9），BOLL(20)/EMA(26) 也要 20~25 根。
不处理的话副图左侧会空出一大块 —— 用户一眼就问「为什么 MACD 左边缺一大块」。

**不要再把这几根裁掉**（早先的做法是生成 40 根「隐藏的预热段」不画）：一旦裁掉，
那几根就既不能看、也不能往左拖，等于在数据里挖了个洞。现在的做法是
**让预热期永远落在视野之外**：渲染窗口在视窗左右各多带 `RENDER_MARGIN`（300）根，
指标算不出来的那 33 根就在这 300 根的余量里，用户根本看不到。

⚠️ 因此 `RENDER_MARGIN` 必须 **大于** 指标预热期（33），否则拖到最左会看到副图左边空一块。

## 实时行情接入层（WS 客户端 / topic 中枢 / 示例页的网关）

行情是**一条会断的长连接 + 好几路 topic**，应用不该每家都重写一遍重连、订阅记账、按帧合并。
所以这一层分三段，各自只负责一件事（`src/realtime/`）：

| 文件 | 负责 | 不负责 |
| --- | --- | --- |
| `client.ts` | 一条连接的生命周期：退避重连 + 抖动、心跳 + 静默看门狗、断线排队、`online` / `offline` 联动、可注入传输 | 不认识 K 线 / 盘口 / 仓位，不知道报文长什么样 |
| `topics.ts` | **合并语义**：同一根 K 线反复推 → 就地改；快照 + 增量 + 序列号对账；仓位 upsert | 不碰网络、不碰图表 |
| `hub.ts` | 订阅记账（重连即重放）、多 topic 分流、**按帧合并**通知、内置 K 线 / 深度 / 仓位 topic 工厂 | 不知道应用怎么画 |

应用侧只需要给三样：**协议**（`subscribe` / `decode`）、**store**（内置三个或自己的）、
**画出来**。示例页把这套跑成了真的：页面自己扮一个 WebSocket 形状的**网关**
（`TerminalPage.createMarketSocket()`），订阅、重连、重放、对账全都是真跑的。

### 为什么必须按帧合并

实测一次 `stack.refresh()` 在 1020 根渲染窗口下 **p50 3.7ms / p95 4.4ms**，而一帧只有 16.7ms
—— 一条流一秒推几十上百次，逐条刷新必然掉帧。中枢把同一条 key 的变更**合进 store、每帧只叫醒应用一次**
（`flush: 'frame'`，也可以给毫秒数）。e2e「按帧合并」钉的就是这条：一帧里推 60 条，`onData` 只到一次，
而 store 里已经是最后那一条。

⚠️ **合并的语义是「状态」，不是「增量」**：同一条 key 一帧内只报最后一次，前面的会被覆盖。
踩过的坑：一帧里「上一根收盘 + 起新的一根」是两条报文，合并后只剩 `append` 那一条 ——
只按 `payload.bar` 改，收盘那根的收 / 高 / 低 / 量就**丢了**，画面上「上一根收在哪」和
「下一根从哪开」对不上。正确做法是遇到 `append` 时从 `change.data.bars()` 里把**最后两根**都同步进页面模型。

### 重连、心跳、排队

- **退避**：`minDelayMs` → `maxDelayMs` 指数增长 + ±`jitter`（避免一批客户端同时回来），
  连上即清零；`maxAttempts` 到顶就 `closed` 并说明原因。
- **心跳 + 看门狗**：定时发心跳；`timeoutMs`（默认 `intervalMs × 2.5`）收不到**任何**消息就判定掉线、
  主动断开走重连 —— TCP 半开和中间设备静默断流只有应用层心跳救得回来。
- **重连即重放订阅**：订阅关系记在中枢里，`open` 一次就重放全部 —— 应用一行都不用写。
- **断线排队**：`send()` 未连接时进队列（默认 64 条，满了丢最旧），连上按序补发。
- ⚠️ **订阅别两头都发**：早期是「订阅时入队 + 打开时重放」，同一条订阅发两遍。现在的口径是
  **未连接时只记账、入库不发**，`open` 时统一重放。
- ⚠️ **`offline` 期间不空转**：关连接前先 `unbindSocket()` 再 `close()` —— 否则断开事件是**异步**回来的，
  它会在你清掉重试定时器之后又排一个，于是离线期间一直在重试（这条踩过）。

### 深度 topic：快照 + 增量 + 序列号

盘口是**唯一错过一条就全盘错**的流，所以 `createDepthStore` 的契约必须守住三条：

1. 增量带 `seq`（或 `prevSeq`），与手里的对不上就报 **`resync`**，应用据此**重取快照** ——
   绝不接着画一本错账；
2. **只 upsert 不删除会穿价**：早先的演示只推 upsert，半分钟不到买一就高于卖一、价差变成负数
   （几张快照的陈价攒在盘口里）。真实网关会把**离开窗口**的档用 `size: 0` 删掉 ——
   演示网关现在也这么推（`depthLadder()` 以现价为锚滚动十档），并且**买卖两侧各删各的**：
   一个价位从买盘迁到卖盘时，旧的那一侧必须先删（store 的 bids / asks 是两张表）。
3. 「重取快照」这件事**别写 `setHint()`**：脚注那一行是共用的（手势反馈、下单结果都在上面），
   而丢包是链路自己的事 —— 一有丢包就把用户刚看到的反馈顶掉，e2e 当场就抓到了
   （「底部时间轴双击」那条因为随机 resync 抢了提示行而失败）。链路健康归脚注那一格
   `#s-feed`（状态 + 重连次数 + 对账计数 + 悬停提示）。

### 页面模型 ≠ store

`CandleStream` 是**链路模型**（只装 WS 推来的那一段），页面的 `this.candles` 是**展示模型**
（多带 `at`、未来空位、往左无限的历史）。两者按 `x` 对齐，WS 负责推最近这一段，页面负责历史与视窗。
补历史 / 重新采样走 `stream.load(bars, { replace: true })` —— 那正是「REST 回补」的位置
（示例页没有 REST，就用自己的采样历史顶上）。

### 给应用的两个方便

- `hub.keyOf(topicName, params)`：参数 → key（`topic:key` 的完整写法），应用**不必自己拼字符串**，
  自定义 topic 改了 `keyOf` 口径也不用两边改；
- 订阅一个没注册的 topic 名会**报错**而不是静默返回 null（拼错一个字，页面会安静地什么都不发生，
  那种 bug 最难找）。

## 示例页

**本仓只有一个示例页** `examples/terminal.html`（完整交易终端屏）—— 用户明确要求把能力
整合到一个综合示例里，而不是摊成几个分页（分页版本在 git 历史里，需要时可取回）。

它仍然守家族的写法契约：`examples/*.html` 一律「**一页 = 一个类**」，内联脚本里不出现
模块级 `function` / `let`，成员顺序 `S*T*F*C*(A|M)*`。写法契约的单一来源是
`ice-web-components/docs/guides/app-pages.md`（本仓示例是伪实时演示，
刷新原本由页面自己的定时器驱动）。

**页面结构**（改之前先看这里）：顶部行情条 / 左侧盘口 / 中间三 pane 图表 + 外壳覆盖层 /
右侧下单面板 / 底部「持仓 · 委托 · 画线记录」。图表外壳（抬头、最新价线、右轴价签、
十字光标两侧 chip）挂在 `stack.holderOf('price')` 上，按几何签名增量同步。

**查询参数**：`?toolbar=simple|custom|replace|none` 切换工具条的三层定制（默认 = 页面
自己的完整定制）。三种定制因此不必各开一个示例页 —— 那会破「一页 = 一个综合示例」这条。

## 端口

e2e 用 **8102**（家族端口表在 `ice-render/AGENTS.md`，新增仓先登记再写配置）。
`reuseExistingServer` 一律 `false`：端口被别的仓占着时要响亮失败。

## 分支与发版约定（家族铁律，2026-09-13 确立）

- **开发**：在临时分支（或 `dev`）上做；`main` 只做集成与发版。
- **发版前**：必须先把开发分支合并进 `main`，**再从 `main` 发版**（跑门禁 → `npm publish`）。
- **禁止**：直接在 `main` 上写实现；也禁止只把改动留在临时分支而让 `main` 停在旧版本。
- 本仓主线名：`main`，远端 `origin`（Gitee）+ `github-origin`（GitHub），两处都要推。
- 提交信息遵循 `@commitlint/config-conventional` 风格；**破坏性变更写在 CHANGELOG 的
  「### 变更（破坏性：…）」小节，提交信息不要用 `!` 标记**。
- `prepublishOnly` 跑 `npm run verify`（不含 e2e，e2e 是发版前的本地动作）。

## 提交前自检

```bash
npm run verify       # types:check → build → jest
npm run verify:full  # 再加 e2e（改了示例页 / 渲染必跑）
```
