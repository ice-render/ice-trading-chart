# Changelog

## [0.4.5] - 2026-09-24

> 跟版内核 0.30.10（类目淘汰不再扫整张表）。1M + 量柱 + 均线：66.7ms → **50.1ms/帧**。

### 变更

- `devDependencies` 跟到 `^0.30.10`；README / AGENTS 的推荐档同步（peer 下限仍是 `^0.30.1`）。

### 测量结论（写进 AGENTS，免得下一轮重走）

1M + 量柱 + 均线那一档剩下的时间：**脚本 1.55s / 3 秒 + canvas 光栅 1.45s / 3 秒**
（样式 0.000s、布局 0.001s；暂停推流整页只剩 0.05s/3 秒）。也就是说
**成本全在「数据更新触发的重绘」**，其中一半是光栅 —— 再往下要动引擎的重绘策略
（整幅重绘 vs 滚动 blit），不属于本包这一层。**远景密度这条线到此收口**。

## [0.4.4] - 2026-09-24

> 接 0.4.3：1M 根 + 量柱 + 均线 **233ms → 66.7ms/帧**（本版 + 内核 0.30.9 的 `xFrom` 合起来；
> 今晚这条线整体：≈3.5s → 2.2s → 750 → 400 → 233 → **66.7ms**）。

### 新增 / 变更

- **派生系列声明 `xFrom`**：`createOverlaySeries` / `createMacdPaneOption` /
  `createRsiPaneOption` / `buildVolumeSeries` 现在都给产物带
  `xFrom: <主系列 id>`（点数与主系列相同时才带）。引擎据此把这些表
  **从类目合并里摘出去** —— 百万点规模下合并是每帧最大的一笔（1M × N 次 `String()` + 哈希）。
  实测 `appendData` 287ms → **7.7ms**。
- 内核跟版 `^0.30.9`。

### 测试

- 本仓 +1（派生系列都带 xFrom）；内核 +3（声明生效 / 点数不符时忽略 / 被引用系列不存在时忽略）。
- `verify:full` = **273 单测 + 38 e2e** 全绿。

### 还剩什么

`#stat-frame` 66.7ms 里最大的一项是**未归名的 `(program)`**（canvas 落墨 / GC 之类），
流水线那侧（归一化 + 布局 + 组件同步 × 每帧 4 次）已经摊平。要再往下压，
先得把 `(program)` 拆开（下一步建议：给 1M 场景做一次带 `--track-allocations` 的采样，
看是落墨还是分配）。

## [0.4.3] - 2026-09-24

> 接 0.4.2：1M 根 + 量柱 + 均线 **750ms → 233ms/帧**（本版 + 内核 0.30.8 的类目域缓存合起来；
> 今晚这条线是 ≈3.5s → 2.2s → 750ms → 233ms）。

### 修复（性能 + 一条过期规避）

- **`seriesData()` 默认不再裁掉首尾 `null`**（要裁显式传 `{ trim: true }`）。
  那条规避来自「上游把 `y === null` 当成 0、折线拉出竖直假线」的旧行为 —— **上游早已修好**
  （`computeEffective` 把断点像素写 NaN、绘制按 NaN 抬笔；实测 `[null,5,6,null,8]` 的 pts
  正好两个断点、`pixelAt` 也是 null）。不裁带来两条实打实的好处：
  ① 「**中间的空洞**没法在应用侧规避」这条老限制不再成立；
  ② **类目表与 K 线逐项对齐** —— 裁掉预热期会让派生系列的类目表跟 K 线错开，
  类目轴只能走「多来源合并」那条慢路（1M × 5 张表 ≈175ms/次；1M 根窗口每帧两趟 ≈180ms）。
  不裁之后它与 K 线逐项相同，直接命中单表快路径。
- 内核跟版 `^0.30.8`：类目（合并）域的**指纹缓存**（同一批存储的两次归一化复用一次合并结果）。

### 测试

- `seriesData` 三条用例按新口径改（默认不裁 / 显式 trim / 整列 null + trim）；
  `createOverlay系列` 那条改成断言「预热期保留成 null 且与 K 线同 x」。
- `verify:full` = **272 单测 + 38 e2e** 全绿。

### 还剩什么

类目域的**增量并集**（内核 `plans/incremental-pipeline.md`）：滚动窗口只动两头，
按 delta 更新并集即可把「每帧两趟 1M 合并」降到 O(1) —— 那是 1M + 派生系列回到
60fps 档的最后一块。

## [0.4.2] - 2026-09-24

> 接着 0.4.1 把「远景密度」的另一半补齐，并**更正 0.4.1 与内核 0.30.6 的帧时间口径**（见那两处的 ⚠️）。
> 1M 根 + 量柱 + 均线：**≈2.2s/帧 → 0.75s/帧**（K 线单系列仍是 16.7ms/帧、CPU 1.4s/3 秒有余量）。

### 新增

- **派生系列在大系列 + 类目 x 时自动开 `virtual`**（`createOverlaySeries` / MACD / RSI）：
  走内核 0.30.7 的**惰性原始点** —— 不建「每点一个 `DataPoint`」、不建逐点像素缓存、
  绘制按像素列抽样。判定写死在一条窄路上：**点数 > 4096 且源数据的 x 是字符串（类目轴）**；
  小图与数值 x 的图**逐字节保持原样**（数值列的虚拟系列会把 `data` 从 option 里摘掉，
  那是「用户显式要求」的语义，不替应用决定）。

### 修复（性能）

- 内核跟版 `^0.30.7`：类目轴上的虚拟折线/面积/散点、虚拟折线不再物化像素缓存、
  类目表合并的「分组 + 顺手建查表口」、数值列支持 `{x, y}` 对象行。

### 测试

- 本仓 +1（压缩模式落墨量按像素列封顶、列内极值不丢）、内核 +6（类目轴虚拟折线 /
  对象行解析 / 虚拟折线不建像素 / 稠密柱子三条 / 合并查表口）。
- `verify:full` = **271 单测 + 38 e2e** 全绿。

### 还剩什么（实测过的下一步）

内核 `plans/incremental-pipeline.md` 的 **P4 同帧批合并** 与**类目域的增量并集** ——
1M 规模下每帧 4 次归一化 + 每帧重扫两张 1M 类目表是现在最大的两块。

## [0.4.1] - 2026-09-24

### 修复（性能：远景密度 —— 不再逐根画/算）

- **K 线压缩模式改成「每像素列抽样 8 根 + 预分配聚合缓冲」**。原来逐根扫可见窗口、
  每帧建一个 `Map<列号, {low,high,up}>`：一屏 40 万根时 **4.7ms/帧**（100 万根窗口实测
  占整帧 27%）。现在列内超过 8 根才抽样（≤8 根时 `stride = 1`，与逐根聚合逐像素一致），
  缓冲是复用的 typed array。

  口径不变：**价格轴量程仍走全量真极值**（`computePriceRange` / `ensureCandleRange`），
  抽样只影响「那一列高低线的端点」；颜色仍取该列**最后一根**的涨跌。

- **内核跟版 `^0.30.6`**（推荐档同步 ≥0.30.6）：上游这一版补齐了**柱子的密度策略**
  （每像素列一根聚合矩形、柱高 < 3px 不建渐变、稠密模式不物化每根像素），
  并给「多来源类目表逐项相同」加了精确快路径。两处合起来：

  ⚠️ **更正（2026-09-24 晚间）**：这里原来写「3516ms → 16.7ms/帧」是**测错了** ——
  那次 rAF 探针撞上了停摆的推流循环。绘制那半确实归零（`fill` 1688ms → 0、每根一个渐变 → 0），
  但 1M 规模下的**帧**由流水线主导（每帧 4 次归一化 + 每次合并两张不同的类目表 ≈38ms）。
  同口径（探针先确认推流仍在推进）的真实数字见 0.4.2 那一版。

### 变更（示例页的观测台）

- `examples/streaming-candles.html` 加两个查询参数开关：`?volume=1`（量柱）、`?ma=1`（均线）——
  远景下真正吃资源的往往是**派生系列**，不打开它们量不出密度策略省了多少；
- 页面 `push()` 现在**两条系列都推**（主 K 线 + 量柱）：真实应用本来就是这么做的，
  也只在这时上游那条「类目表逐项相同」的快路径才认得出来（实测只推主系列时两张表第一项就差 109 根）。

### 测试

- `tests/candlestick.test.ts` 覆盖压缩模式的抽样口径（尖峰仍在、颜色取该列最后一根）；
- 上游 `tests/components/series.test.ts` 新增三条覆盖柱子稠密模式（触发条件 / 每列一根聚合矩形
  且尖峰不丢 / 不物化像素缓存且命中仍准）。

`verify:full` = **271 单测 + 38 e2e** 全绿。

## [0.4.0] - 2026-09-24

> 这一版把上一轮的工具条改动与两处实时流性能修复一起发出去。
> 两处性能修复合起来：**10 万根窗口的整帧 JS 从 ~7ms 降到 ~1.5ms**，
> 30 万根 20fps → 60fps、60 万根 10fps → 60fps、100 万根仍是 60fps（实测见下）。

### 依赖

- **`devDependencies` 跟版 `@damoqiongqiu/ice-chart@^0.30.5`**，README 的「推荐内核」同步到
  **≥ 0.30.5**（`peerDependencies` 的下限仍是 `^0.30.1`：这只是性能改进，不抬门槛）。
  0.30.5 修的是**上游那一趟**：类目查表口一被视窗裁剪就退回自建索引表 ——
  滚动看盘（轴只显示窗口里那一段）每帧要为整张类目表重建一张 Map。
  上游尺子 `measure-pipeline.mjs` 新加的「环形 + 视窗」档位：10 万点每 tick **1.6ms → 0.3ms**。
  对本包的直接效果：`appendData` 每根 **2.8ms → 0.8ms**（10 万根窗口）。

### 修复（性能：大窗口实时流）

- **列存不再「每根重建整个窗口」**。`candleColumnsFor` 的复用判定带着
  `cached.series === series`，而 `series` 是归一化产物、**每次 `applyOption` 都是新对象** ——
  缓存永远判不中，于是每推一根就重扫整个窗口：10 万根 **13ms/次**，占满一帧的大半
  （30 万根时页面掉到 20fps、60 万根 10fps）。两处改法：

  - 复用判定只认**存储**（`series.raw` / 数据数组）自己的状态（容量 / 起点 / 长度）；
  - 「环没滑动、长度也没变」时用 O(1) 的 `refreshTailRow` 复查**最后一根** ——
    实时流里正在形成的那一根会原地改值，这一条保证不用为了正确性牺牲增量。

  实测（同一台机器、`examples/streaming-candles.html`，帧间隔中位数）：

  | | 修之前 | 修之后 |
  |---|---|---|
  | 本包列存维护（3 秒 / 10 万根窗口） | **2350ms**（180 次全量重建 × 10 万行） | **24.7ms**（180 次环滑动，0.13ms/次） |
  | 30 万根 | **20fps**（50ms/帧） | **60fps**（16.7ms/帧） |
  | 60 万根 | **10fps**（100ms/帧） | **60fps**（16.7ms/帧） |

  用例：`candle-streaming` 新增三条 —— 「增量维护 = 全量重建（滑动 + 追加 + 尾根改值之后
  逐根逐列一致）」「series 身份换了照样复用」「同一根原地改值只复查尾部」。

  ⚠️ 这条**推翻了 2026-09-23 的旧结论**（「增量路径比全量重建还贵，别再修缓存判中」）：
  那次 A/B 拿 `appendData` 的单次调用时间去比**整帧**时间，方向被带偏。新账与更正后的
  结论写进了 `AGENTS.md`「大窗口实时流：列存复用」。

- `examples/streaming-candles.html` 多一个 **`#stat-frame`（帧间隔中位数）**：大窗口下
  它是唯一能一眼看出「列存维护在不在这一帧里」的数（`?window=300000` 最直观）。

### 测试（刻度密度的带子改成 17~47px，实测口径）

- 「刻度密度」那条 e2e 原来卡「一档 > 18px」，但**目标 27.5px 的 1/2/5 梯子最坏就有
  √2 的差**（19.4px），再叠上引擎对数据域的取整，实际能落到 **17.70px**（实测命中过两次，
  与今天的两处性能改动无关：改动前那次也是同一个值）。门禁与文档的口径一起改成
  **17~47px**，并把「为什么下沿比理想的 19.4 还低」写进注释与 AGENTS。
  真正坏掉的密度（退回固定 5 档 = 57~71px，或挤到 10px 以下）照样逃不掉这条断言。

### 变更

- **工具条从示例页搬进库里，并补上「占带」**：`createChartToolbar` 现在就是终端页那条工具条
  的本体（周期 / 类型 / 指标 / 画线 / 显示），页面只剩业务回调 —— 切周期**重订阅 K 线流**、
  参数改动重算指标、画线动作动 `drawing`。配套三件：

  | 能力 | 入口 | 为什么 |
  |---|---|---|
  | 占带 | `createPaneStack({ toolbar })` / `stack.setToolbar(slot)` | 工具条是图表的一条带：pane 高度从容器高度里扣掉它。悬浮会盖住最上面那几根 K 线 |
  | 动态文字 | 自定义项的 `label` 可以是 `(ctx) => string` | 周期按钮上的 `5m` 这类「跟着状态走」的字；按钮 `title` 按签名更新（换语言跟着换） |
  | 面板自收 | `ctx.closeMenu()` | 面板内容归应用，「点完要不要关」应用说了算（指标多选要连着勾几个） |

  主题色走 `--ice-toolbar-*` 变量（与盘口同一套姿势）；换肤 / 换语言**不重建 DOM**，
  `update()` 按签名去重（每帧刷新也只在真变了时才写 DOM）。

- **示例页删掉自绘那条工具条**：`.chart-bar` 只留页签与右侧读数，工具条本体由库渲染并
  **占带**在 `#panes` 顶部；五张下拉改成库的**自定义面板**（面板里的行与内置项共用一条命令
  路径：`setIntervalKey` / `setChartType` / `setIndicator` / `setDrawingTool` / `setDisplay`）。
  左侧画线图标条与工具条菜单现在是同一个 `setDrawingTool()`（Esc 退出也走它）。

  三种定制**不另开示例页**（本仓只有一个示例页），改用查询参数：
  `?toolbar=simple`（第 1 层只减项）/ `custom`（第 2 层加一个「预警」自定义项）/
  `replace`（第 3 层整条替换，位置仍由占带给）/ `none`（完全不要）。

### 修复

- **工具条面板里勾一个指标，下拉就关了**（面板内的点击冒泡到 document 的「点空白处收起」）——
  设计稿要求多选时保持展开，现在面板根上的点击被拦住，并有用例钉住。
- **换语言后按钮的悬停提示还是旧语言**（`title` 只在建按钮时写过一次）—— 改成按签名更新。

### 测试

- 工具条单测 9 → 16：动态 `label` + `title`、`ctx.closeMenu`、面板内点击不冒泡、Esc 收起、
  `setOptions({ items })` 时开着的那张面板按同一个 id 重新挂上、主题变量换肤不重建 DOM。
- 占带：`panes` 单测 +2（带占容器顶部、pane 高度扣掉它、`setToolbar` 运行中换 / 拆带）。
- e2e：`toolbar.spec.ts` 重写成「库组件在终端页上的行为」（切周期 / 指标 / 画线 / 占带 /
  三层定制 / 换语言换肤不重建 DOM，6 条），`terminal-pan.spec.ts` 里 15 处旧选择器换到新 DOM。

  全绿：`verify:full` = **267 单测 + 38 e2e**。

### 文档

- `AGENTS.md`：顶部工具条一节重写成「库组件（三层定制）+ 占带」，记下四条踩过的坑
  （占带后要重算刻度密度、每帧只 `update()` 不重填面板、`label` 可以是函数、面板自收）。
- `plans/chart-toolbar.md`：状态改为**已落地**；§6.5 的「仍然没做」改成做完清单与落地时的
  修正，§7 三个待定项定案。

### 新增

- **工具条第 2 层补上「自定义面板」**（`{ id, label, menu: (ctx) => HTMLElement }`）：容器、定位、
  主题、点外部关闭都由库负责，**面板内容归应用**，且**每次打开时重建** —— 参数输入框、
  语言/主题这类"要放控件、不是简单开关"的项因此也能用库的触发器。

  这条是**删掉终端页自绘工具条的前置**：盘点下来库当时表达不了页面已有的四件事
  （周期收藏 + 五个菜单 ✅；**指标参数输入框 / 语言·主题·配色方案 / 画线的删除·清空** ❌），
  直接删会丢功能 —— 这正是设计稿里"并存对照一轮再删"要挡住的东西。补了 `menu` 之后
  三类都能表达；剩下的迁移（把页面判断搬进宿主回调、再删自绘）见 `plans/chart-toolbar.md` §6.5。

  验证：工具条用例 8 → 9 条（新增"自定义面板：每次打开重建、再点收起"）；
  `verify:full` 257 单测 + 35 e2e 全绿。

### 测试

- **工具条的行为回归进 e2e**（新开 `e2e/toolbar.spec.ts`，3 条；e2e 总数 32 → 35）。

  三条量的都是**页面状态真的变了**（`window.__page` 上的字段），不是"按钮亮没亮" ——
  工具条如果只是自己变了样、没把命令送到图上，这三条必须红：

  | 用例 | 断言 |
  |---|---|
  | 切周期 | `intervalKey` 变 1H；x 轴类目**真的换了**（不是只改文字）；库那条与自绘那条的文字都对上 |
  | 指标开关 | `indicators.ma` 变 false；图上**叠加数减少**；主图图例行条数也跟着减少 |
  | 画线工具 | `drawing.mode()` 变 `trend`；页面那套工具条上同一个工具进入激活态 |

  顺带记两条写测试时踩的：① 页面那套工具条的激活类名是 `active` 而不是 `on`（断言要按对方的
  口径写，别按自己的习惯）；② 新 spec 独立成文件，不往 `terminal-pan.spec.ts` 里堆 ——
  那份文件已经 1400 行，主题也不同（它管平移与光标）。

### 新增

- **工具条并存对照**（`?toolbar=lib`）：把库里的 `createChartToolbar` 挂到自绘那条下面，
  默认页面**一行不动**（不带参数时行为与之前完全一致）。

  这一步的目的不是"上线新工具条"，而是**验证库的 API 够不够表达页面已有的命令**。
  所以宿主回调**不复制任何业务逻辑**：读状态从页面读（`intervalKey` / `indicators` /
  `drawing.mode()` / `magnet`…），写意图就点一下页面已有的控件（`[data-interval]` /
  `[data-ind]` / `[data-tool]` / `[data-display]` / `[data-type]`），让它们照旧走那条
  委托处理器 —— **等价性因此是结构性成立的（同一条代码路径），不是"看着一样"**。

  实测（`?toolbar=lib`）：库那条渲染出 5 项、周期文字 `5m`（从页面状态读来）；
  点它的「周期 → 1H」后**页面自绘那条的标签也变成 1H** —— 意图确实走的是页面的命令路径。
  无 pageerror；`verify:full` 257 单测 + 32 e2e 全绿（默认页面未受影响）。

  ⚠️ 接线时踩到一个本仓踩过好几次的坑：我最初把「显示」项的 id 写成 `log` / `percent`，
  而页面真实的是 `magnet` / `volumeMa` / `sessionLines` —— **点了会静默无效**。
  已改成与 `data-display` 取值一一对应，并在注释里写明这条纪律。

  仍未做：删掉自绘那条（设计稿要求对照一轮之后再删）、三种定制的示例页、
  3 条 e2e（周期 / 指标 / 画线）、以及由图表给出槽位的「占带」集成。

### 新增

- **图表工具条**（`createChartToolbar`）：周期 / 图表类型 / 指标 / 画线 / 显示。

  用户 2026-09-23 定了两件事：**工具条是图表的一部分**（不是宿主页的），**但要允许使用方定制**。
  设计稿见 `plans/chart-toolbar.md`，这一版落地的是**三层定制**的骨架：

  | 层 | 入口 | 给谁用 |
  |---|---|---|
  | 1 · 声明式 | `items: ['intervals', 'display']` —— 数组顺序即显示顺序，不写就不显示 | 只想少放几项 |
  | 2 · 自定义项 | 同一个数组里混 `{ id, label, icon, onClick(ctx), active(ctx) }` | 想加自己的按钮 |
  | 3 · 整条替换 | `render(ctx) => { element, update }`，或 `show:false` 自绘 | 想整条自己画 |

  `ctx` 是承诺给使用方的正规入口（`chart` / `stack` / `messages` / `theme` / `refresh`），
  自定义项因此**自动跟着主题与语言走**，不必自己写色值和文案。

  **铁律**：工具条不自己存图状态 —— 周期、类型、指标显隐的真相永远是图表 option，
  内置项只做「发意图（`onIntervalChange` / `onToggleIndicator` / …）+ 读状态」。
  所以外部改了 option，下一次 `update()` 会自己跟上，而且**不重建结构**（重建会让使用方手里
  的节点引用失效）—— 这两条都有用例钉住。

  一致性照抄本仓已跑通的四套：`orderBook` 的「样式注入一次 + CSS 变量」、`messages` 目录
  （新扩 `toolbar` 段，zh/en 各一套）、「结构一次 + 签名去重」、「组件只管呈现、交互回调给应用」。
  `messages.ts` 文件头原来写着「工具栏是应用的事」—— 那条理由已随这次的定位决定改掉。

  验证：8 条用例（默认顺序 / 只减项 / 自定义项拿 ctx / 外部状态跟上且不重建 / 周期面板
  / 指标面板取反 / 整条替换 / `show:false`）；`verify:full` 257 单测 + 32 e2e 全绿。

### 新增

- **逐笔成交的大单强调**（币安那栏 `Trades` 的观感）。方向着色本来就有（价格列按买卖方向）；
  这次补的是**量的大单强调**：窗口内量 ≥ 中位数的两倍时，该行加粗 + 量列淡底（价格仍保持方向色）。

  用**相对口径**而不是写死绝对值 —— 不同品种的量级差几个数量级（SYN 与 BTC），
  写死阈值必然在另一头失效。

  顺带修了演示数据：原来成交量是**均匀分布**（0.002~0.6），于是"中位数 × 2"几乎永远够不到，
  大单强调**一次都不会触发**（实测 14 行 0 条）。改成**重尾**（多数小单 + 约每 8 笔一笔大单）——
  既更接近真实成交流，也让这个功能真的看得见（改完实测 14 行 2 条）。两个成交产生点共用
  `TerminalPage.tradeSize()`，不重复写分布。

### 新增

- **盘口：三态视图 + 聚合精度**（对齐币安合约页盘口那三件里的后两件；「合计」列本来就有）。

  都按本组件的既有约定做：**结构只建一次**（切视图只切 display）、**变更按签名去重**、
  **纯逻辑抽成可单测的函数**、**交互外壳归应用**（组件只给 API，面板上的按钮由页面画）。

  - `setView('both' | 'bids' | 'asks')` + `view()`：仅买 / 仅卖只切两侧容器的显隐，不重建 DOM；
  - `setPriceStep(step)` + `priceStep()`：聚合在组件内做，应用**照样推原始档位** ——
    切精度不需要行情源配合，也不存在「聚合后的数据被当成原始数据再聚合一次」的问题；
  - `aggregateLevels(levels, step, side)`：纯函数（导出可单测）——买单向下取整、卖单向上取整
    （都不穿过自己的价格），同格量相加，输出顺序仍是「最优价在前」；
  - 没给 `priceFormat` 时，默认格式**跟着步长走**（步长 1 不再显示 `742.00` 这种冗余小数）。

  终端示例页接了面板头的小工具（双向 / 仅买 / 仅卖 + 精度下拉），并让页面的价格格式
  在粗步长下退到 0 位小数。验证：19 条盘口用例（15 旧 + 4 新）+ `verify:full`
  245 单测 ×（本次新增 4 条）+ 32 e2e 全绿。

### 新增

- **主图叠加的图例行**（对齐币安合约页的 `MA(7) 731.98  MA(25) …` 那一行）：终端示例页在主图
  OHLC 抬头下面多了一行「色块 + 名称 + 当前值」，点一下切换该条线的显隐（走 `chart.toggleSeries`）。

  两个实现要点：① 值**不重算** —— 直接读图表 option 里那几条线（与画出来的是同一份 `data`），
  取 `reading.index` 那一项，下标对不上时退到最后一个有效值（所以光标停在哪根就报哪根的值）；
  ② 显隐状态读 `chart.norm.series[].hidden`（归一化后的真相），页面不另存一份。
  验证：页面上 2 条 MA 正常渲染、点击第一条后 `k__ma7` 变暗且另一条不受影响、无 pageerror；
  `verify:full` 245 单测 + 32 e2e 全绿。

## 0.3.1 - 2026-09-22

### 修复

- **环形（列存）形态下蜡烛静默不画**：本包自己的列存原来照 `option.data` 建，
  而环形形态下原始数据归存储所有、`data` 已被摘掉 —— 于是列是空的（实测 `validCount === 0`），
  蜡烛一根都不画；画布上还有坐标轴，示例页冒烟**照样绿**（只看"有没有墨"）。
  现在取数一律走公开访问器（`series.pointAt(i).raw`），见下一条。
- 抬头读数里「最后一根有效 K」的判定同样不再看 `option.data`（同一个坑）。

### 变更

- **列存按系列增量维护**（`candleColumnsFor`）：缓存按存储对象做键，
  环滑动按 `raw.start` 平移几格（`copyWithin` + 只解析新进来的），尾部追加只解析新增的。
  类目索引表存**相对偏移**，滑动不必重写整张 Map。于是「追加一根」是 O(1)，
  而不是每 tick 重扫整个窗口；1 万根窗口的数据路径 **4.3ms → 1.3ms**（其中图表流水线 ~1.1ms，
  两条路都要付；存储部分是 0.13ms）。
  注意：增量之后极值可能陈旧（滑动会淘汰极值那根），要精确极值先调 `ensureCandleRange()`。
- **派生的成交量系列也开列存**（惰性原始点）：不建「每根一个 `DataPoint`」，
  1 万根省 ~0.6MB、100 万根省 ~60MB；量柱渲染与量程不变（实测终端页量柱像素正常）。

## 0.3.0 - 2026-09-22

> 兑现上一版留下的那半句：**大窗口 K 线的实时追加也走环形缓冲**。
> 同时把 K 线默认切到上游列存（不建每根一个 `DataPoint`），并补了一个 A/B 示例页。

### 新增

- **大窗口 K 线流：环形缓冲 + A/B 实测页**（`examples/streaming-candles.html`）。
  窗口 1 万根，页内可以直接切换「环形追加（`appendData` + `maxPoints`）」与
  「全量 setOption」两条路，并显示数据路径 / 视窗推进两个数。
- `TradingChartExtras.priceRange`：**显式价格轴量程**。窗口式数据源（环形 / 分块）下
  `series.data` 会被摘掉，库没法再扫原始数组算影线极值 —— 量程由应用给；
  运行中调整走公开的 `setDomain('y', [min, max])`。

### 变更

- **K 线默认开上游列存**（`toTradingOption` 补 `virtual: true`，用户显式 `false` 时不覆盖）：
  走「惰性原始点」—— 不建每根一个 `DataPoint`，原始数据仍按引用保留
  （提示框 `params.data`、本包字段解析照旧），像素缓存照旧。
  实测 10 万根：**堆 24.8MB → 18.9MB**。
- 抬头读数（`readout.ts`）改走 `pointCount` / `pointAt(i)` / 列存 `valid`，
  不再直读 `series.points`（列存下它是空的）。

### 实测（10 万根窗口，真机 Chromium；配套 ice-chart 0.30.1 的三处通用修复）

| 口径 | 环形追加 | 全量 setOption |
|---|---|---|
| 数据路径（追加 + 域） | **0.13ms/tick** | ~1.4ms/tick（含物化 1 万个点） |
| 端到端每 tick（含流水线） | **1.7ms** | 同量级 |
| 堆 | **5.9MB** | 9.1MB |

地板是「数据一变就重跑归一化 + 布局」这条流水线（~1.6ms/次），与数据怎么存无关 ——
要再往下压得让上游支持增量归一化 / 布局（另立一项，不算这次的欠账）。

### 变更

- **K 线默认开上游的列存（ice-chart 0.30 的 `virtual`）**：`toTradingOption` 现在会给
  candle 系列补 `virtual: true`（用户显式写 `virtual: false` 时不覆盖）。它走的是上游新增的
  **惰性原始点**存储：**不再建「每根一个 `DataPoint`」的数组**，原始数据仍按引用保留
  （提示框 `params.data`、本包自己的字段解析都照旧），像素缓存照旧。
  实测 10 万根：**堆 24.8MB → 18.9MB**（省下的就是 DataPoint 那一份，约 60B/根），
  命中与重绘不变（p50 16.5ms / 0.00ms）。
  两条纪律：读点一律走 `pointCount` / `pointAt(i)` / `xValueAt(i)`（列存下 `points` 是空的，
  抬头读数已经改过来）；`renderAs: 'line'/'area'` 不套用这个默认（那会变成内置数值列系列，
  类目轴不吃）。

## 0.2.0 - 2026-09-22

> 两件事：**K 线列存**（10 万根 K 线命中 223ms → 0.00ms、视窗变化 254ms → 16.7ms）
> 与**跟版 ice-chart 0.29**（peer 下限从 ^0.28 提到 ^0.29，因为下游依赖 0.29.1 的
> 类目轴修复：10 万类目时坐标轴一次渲染 3716ms → 0ms）。
> 契约与纪律见 AGENTS 的「K 线列存」一节。

### 新增

- **K 线列存：四个价解析一次，渲染 / 命中 / 量程共用（10 万根 K 线的账）**。
  新增 `src/series/candleColumns.ts`：`open/close/low/high` 四条 `Float64Array` + `valid`
  （读不出的根不画，不补值）+ 类目索引表 + 影线极值；缓存键是「数据数组 + 字段名签名 + 长度」，
  所以换数组 / 追加会自动重建（等长原地改值要调 `invalidateCandleColumns`）。
  改动点：
  - 渲染与命中都**只在可见窗口内**工作（`visibleRange()`：类目轴 domain 的首末类目 → 下标，O(1)）；
  - 命中不再「扫全量 + 每根建一个 Rect」，也不再每帧每根重新 `readOhlc(raw)`；
  - 价格轴量程与渲染**共用同一份列**（同一趟扫描，不再各解析一遍）；
  - 一屏超过「每根 0.25 像素」时进**压缩模式**：每像素列一根高低线（真实极值，颜色取该列最后一根）；
  - 公开 `candleRectAt(i)`（单根几何，给外壳读数 / 对齐断言用）。
  实测 10 万根（同一台机器、同一把尺子、同一份数据）：

  | 口径 | 改前 | 改后 |
  |---|---|---|
  | 命中 p50 / p95 | **223.4ms / 446.8ms** | **0.00ms / 0.10ms** |
  | 视窗变化 + 重绘 p50 | **253.7ms** | **16.7ms** |
  | 首帧（K 线组件部分） | 3722ms | **15ms** |
  | 堆 | 24MB | 25MB（多的 4MB 就是那几条列） |

  ✅ **上游瓶颈已修（ice-chart 0.29.1，本仓 peer 下限因此是 `^0.29.0`）**：首帧剩下的 ~3.7s
  在 `ice-chart` 的 x 轴上 —— `Axis.tickEntries()` 对**每个类目**都调 `scale.map()` +
  `formatTick()`，而类目轴的 `map` 内部是线性查类目。0.29.1 改成「抽稀掉的刻度不进表 +
  `BandScale` 类目查表缓存」：**axisX 3716ms → 0ms、首帧 3737ms → 17ms**（10 万根实测）。
- **实时行情接入层（WebSocket）**：行情是「一条会断的长连接 + 好几路 topic」，这一层把重连、
  订阅记账、按帧合并、快照对账一次做完，应用只给协议与画法。三个文件各管一件事：
  - **`createRealtimeClient(options)`**（`src/realtime/client.ts`）：一条连接的生命周期 ——
    指数退避 + 抖动重连（`minDelayMs` / `maxDelayMs` / `factor` / `jitter` / `maxAttempts`）、
    心跳 + **静默看门狗**（`heartbeat.timeoutMs` 默认 `intervalMs × 2.5`，TCP 半开靠它救）、
    断线期间**排队**（`queueLimit`，满了丢最旧）、`online` 立刻重连 / `offline` 不空转、
    状态与事件（`state` / `open` / `message` / `error` / `close`）、传输可注入（`createSocket`）。
  - **topic store**（`src/realtime/topics.ts`）：`createCandleStream`（同一根反复推 = 就地改、
    换 x = 接新的、更旧的丢弃、`load()` 按 x 去重合并）、`createDepthStore`（快照 + 增量、
    `prevSeq` / 连续 seq 对账、**跳号报 `resync`**、每侧按档数封顶）、`createPositionStore`
    （`symbol + 方向` upsert、`size <= 0` 摘掉）。
  - **`createRealtimeHub(options)`**（`src/realtime/hub.ts`）：**订阅记账**（连上就自动重放全部订阅）、
    多 topic 报文分流（不是订阅过的 key 直接丢）、**按帧合并**通知（`flush: 'frame'` 或毫秒数；
    同一条 key 一帧内只报最后一次 —— 通知的是「状态」不是「增量」）、
    内置 `createCandleTopic` / `createDepthTopic` / `createPositionTopic` 三个工厂，
    也支持应用自己写 `{ name, keyOf, subscribe, decode, apply }`；
    新增 `keyOf(topic, params)` 让应用不必自己拼 store 的 key，订阅未注册的 topic 名会报错。
  - **示例页**：页面自己扮一个 WebSocket 形状的**网关**（`createMarketSocket()`），四种 topic 全接
    （K 线 / 深度 / 仓位 / 成交，最后一条是页面自己写的自定义 topic），盘口改走「快照 + 增量 + 删除」，
    持仓表与账户面板改读 store；脚注最右一格是链路状态（`● 行情 已连接`），**点一下模拟断线**
    可以看客户端自己退避重连并重放订阅。
  回归网：`tests/realtime-client.test.ts`（8 条）、`tests/realtime-topics.test.ts`（8 条）、
  `tests/realtime-hub.test.ts`（6 条）+ e2e 四条（重连重放 / 按帧合并 60 推只叫醒一次 /
  深度跳号重取快照 / 四条流各回各家）。

- **i18n 接入点**（外部使用方确认「能不能方便地接进去」暴露出的缺口）：
  - **文案目录** `TerminalMessages`（`src/messages.ts`）：库自己渲染的每一句话都在这里 ——
    提示框四价、盘口表头三列、指标序列名（MA/EMA/BOLL/MACD/DIF/DEA/RSI）。预设
    `ZH_TERMINAL_MESSAGES` / `EN_TERMINAL_MESSAGES`，`resolveTerminalMessages('en' | 片段)`
    **按分组深合并**；入口是 `createTradingChart(…, { messages })`、
    `createOrderBook({ messages })`、`createOverlaySeries / createMacdPaneOption / createRsiPaneOption(…, { messages })`。
  - **数字格式化**：`TerminalNumberFormat`（`createTradingChart(…, { numberFormat })` 同时管价格轴刻度与
    提示框），成交量另有 `volumeFormat`；新增 `createIntlNumberFormat(locale)`
    （`42 300.5` → `de-DE` 的 `42.300,50`），环境没有 `Intl` 时退回默认格式。默认仍是千分位 + 固定小数位。
  - **盘口**：`createOrderBook({ messages })` 与新增 **`setMessages(messages, labels?)`**（换语言只改表头文字、
    不重建结构；显式 `labels` 仍优先，用于「目录 + 单位」这种组合）。
  - **示例页**：新增「显示 → 语言：中文 / English」。页面自己的文案走 `PAGE_MESSAGES` 目录 +
    `data-i18n` / `data-i18n-title` 属性，JS 里动态拼的走 `t(key, params)`（`applyI18n()` 一次刷）；
    同时把库的 `messages` 喂给图表 / 盘口 / 指标构造，于是**库渲染的字与页面渲染的字一起换**。
    演示页没做 i18n 的是下拉选项文字与 `setHint()` 的动态提示（应用自己的业务文案，接法相同）。
  回归网：`tests/messages.test.ts`（目录 / 深合并 / locale 数字格式 / 提示框 / 价格轴 / 盘口表头）、
  e2e「i18n」（库文案 + 页面文案一起换、可换回）。


- **终端主题（一套 token 驱动三处外观）**：新增 `TerminalTheme` 与
  `DARK_TERMINAL_THEME` / `LIGHT_TERMINAL_THEME` / `resolveTerminalTheme()` /
  `terminalThemeToChartTheme()` / `applyTerminalTheme()`（`src/theme.ts`）。
  - **图表**：`terminalThemeToChartTheme()` → `createPaneStack({ theme })` → ice-chart 的
    `ChartTheme` → **ice-chart 的主题桥**推到 **ice-render 的引擎主题**（应用不需要自己调
    引擎的 `setTheme`，画布底色 / 交互外壳 / 字体一起走）；
  - **盘口**：`createOrderBook({ theme })` / `book.setTheme(theme)` —— 面板、文字、悬停底
    写成组件根节点上的 `--ice-book-*` 变量，涨跌色取自 token；
  - **页面外壳**：`applyTerminalTheme(theme)` 把 token 写成一组 CSS 变量
    （`TERMINAL_THEME_VARS` 是变量名表），页面样式表只写 `var(--xxx)`。
  - `createPaneStack` 新增 **`setTheme()`**：主题是在建栈那一刻解析的，换肤必须显式重设
    （改 `option.theme` 不生效；重建整摞 pane 会丢缩放窗口与画线）。
  - 示例页「显示」面板里加了**深色 / 浅色盘面**切换，演示这三处一起换。
  回归网：`tests/theme.test.ts`、`tests/panes.test.ts` 的 `setTheme`、`tests/orderBook.test.ts`
  的主题那条、e2e「盘面主题」。


- **盘面观感与布局对齐主流合约交易所的盘面**（用户要求「在外观和布局方面做一次深入同步」）：
  - **布局**：顶部「行情跑马灯 + 交易对抬头（标记价 / 指数价 / 资金费率与倒计时 / 24h 高·低·量·额）」；
    三栏「图表 ·（订单簿 + 最新成交）·（下单 + 账户）」；底部「持仓 / 当前委托 / 画线记录」
    改成**选项卡**（选中项一条品牌黄），并且**横跨图表 + 盘口两栏**，右侧下单栏同高。
  - **配色**：一套深色盘面变量（底 `#0b0e11`、面板 `#181a20`、边框 `#2b3139`、弱化 `#848e9c`、
    强调 `#f0b90b`、涨 `#0ecb81`、跌 `#f6465d`），图表主题吃同一套；**默认涨绿跌红**；
    报价千分位 + 两位小数；均线用黄 / 紫 / 灰。
  - **盘口**（`src/orderBook.ts`）：新增表头（价格 / 数量 / 合计）与**合计列**（从最优价往外累加）；
    新增 `labels` 选项；「最新成交」列表（价格 / 数量 / 时间，最近 14 笔）。
  - **下单面板**：全仓 / 逐仓 / 杠杆胶囊、限价 / 市价选项卡、委托价格带「最优价」、
    **按可用保证金百分比的菱形滑杆**（0/25/50/75/100，可拖可点）、杠杆滑杆、止盈止损 / 只减仓、
    买入做多 / 卖出做空；**账户面板**（保证金比率 / 维持保证金 / 保证金余额 + 划转 / 买币 / 兑换）。
  - 回归网：`tests/orderBook.test.ts` 的两条（两侧颜色、列名与合计列）+ e2e 全量。
- `createOverlaySeries` 新增 `palette`（均线配色可由调用方给，示例页用合约盘面的黄/紫/灰）。

### 变更

- **报价格式化加了千分位**：`formatPrice(value, precision)` 现在返回 `82,532.40` 这种写法
  （固定小数位 + 千分位）。刻度标签的补位预算要按最长标签给（示例页 `axisLabelChars` 9 → 10）。
  不传 `precision` 时仍是老口径（最多 6 位、去尾随 0、无千分位）。
- **盘口两侧的颜色口径改为「卖盘 = 跌色、买盘 = 涨色」**（原来反着取）：合约盘面上卖盘偏红、
  买盘偏绿；换涨跌配色时两边跟着翻。默认色也更新为 `#0ecb81` / `#f6465d`。


- **一把梭哈的五件套**（继续往主流看盘软件对齐）：
  1. **图表类型**（蜡烛 / 空心蜡烛 / 折线 / 面积）：新增 `TradingSeriesOption.renderAs` ——
     渲染类型与交易语义分开，切类型不丢影线量程 / 成交量 / 抬头（`isCandle()` 认 `renderAs`，
     只有输出系列换成 `line` / `area`）。
  2. **价格轴右键菜单**（右侧标尺上右键）：自动量程 / 线性 / 对数 / 百分比。百分比以
     **可见窗口第一根**的收盘为基准、只改标签口径（刻度位置仍是价格口径，这是引擎能力的边界）。
  3. **指标参数可改**：「指标」面板里直接改 MACD 快/慢/信号、RSI 周期、MA / EMA 两条、BOLL 周期，
     副图、图例、抬头立刻跟着走（`change` 事件，不是每敲一个字符就重建）。
  4. **左侧画线图标条**：画线工具竖排在图表左边缘（趋势线 / 水平线 / 垂直线 / 区间矩形 +
     删除选中 / 清空），选中态高亮、`title` / `aria-label` 齐全、Esc 退出；顶部「画线」下拉保留。
  5. **十字光标磁吸 + 均量线 + 会话分隔线**：磁吸把横线与价签吸到这一根最近的开高低收
     （横线的 y 也按吸附价反算）；量图上加 MA5 / MA10（首尾读不出量的点裁掉，避免折线把
     null 当 0 画假线）；每天第一根上一条极淡的会话分隔线（画在外壳覆盖层里，关掉时清干净 DOM）。
  回归网：`tests/option.test.ts` 的 `renderAs` 两条 + e2e 五条（图表类型 / 右键菜单 /
  指标参数 / 图标条 / 三个开关）。

- **顶部工具条改成图标按钮**（用户反馈「小工具条都是用的图标」）：周期 / 指标 / 画线 / 显示
  四个触发按钮换成内联 SVG 图标（时钟 / 柱状 / 铅笔 / 滑杆，`stroke` 走 `currentColor`），
  保留悬停提示（`title` + `aria-label`）；**图标说不出「开着没」**，所以指标开着、画线工具武装着
  （或已经画过东西）、显示改了默认值时按钮右上角点一个 accent 小圆点；**周期那一个保留文字**
  （图标 + 当前周期 `5m`）——周期本身是文字信息。收藏周期那一排、四组面板的行为（多选保持展开、
  点空白 / Esc 收起、事件委托）都没动。回归网：e2e「顶部工具条是图标按钮」。

- **键盘视图控制**（主流看盘软件的口径）：`←` / `→` 平移时间轴（一屏的 10%）、
  `↑` / `↓` 缩放价格轴、`+` / `-`（`=` / `_` 同义）缩放时间轴、`Home` 回到最新；
  表单里与带修饰键时不抢键。落地是两个新的公开导出
  `panTimeAxis(chart, { bars? })` / `zoomTimeAxis(chart, { factor, anchorX? })`
  （`src/axisView.ts`）：都和鼠标手势共用同一套引擎夹取（`minBarSpacing` / `minSpan`），
  照常抛 `pan:change` / `zoom:change`，页面的「跟盘 → 手动」状态机、量程重算、渲染窗口收放
  一行都不用重写；平移走 `source: 'pan'`（只要求与数据交叠 1/4 个窗口，不会把平移夹成缩放）。
  引擎自带的那套键盘（←/→ 挪活动点、↑/↓ 换系列）在示例里关掉（`interaction.keyboard: false`）——
  它会把准星挪到数据点上，和「准星跟指针」冲突。回归网：`tests/axisView.test.ts` 四条 +
  e2e「键盘」那条。
- **每块 pane 左上角一行图例 + 主图水印**：主图是「交易对 · 周期 + 开高低收量 + 涨跌」，
  成交量是「Vol + 当前量」，副图是「MACD 12 26 9 + DIF/DEA/柱值」（副图两条线用它们自己的颜色，
  柱值与涨跌同步上色）；主图正中一枚大号淡水印。文字在 `renderReadout()` 里按签名增量写、
  位置在 `syncChromeBox()` 末尾按各 pane 几何贴。副图的指标值按「数据长度 + 周期 + 副图类型」
  缓存，指针移动不会每次重算 O(n) 的 EMA。
- **修复：抬头 / 图例在没有悬停时是空的**。`createOhlcReadout()` 的兜底取的是
  `points.length - 1`，而视窗停在最新一根时渲染窗口尾巴上挂着几百个「未来空位」
  （只有类目、没有 OHLC）—— 兜底正好落在空位上，`read()` 返回 null。现在兜底会往前找到
  **最后一根读得出 OHLC 的**（按点数缓存，数据刷新不重扫）。回归网：
  `tests/readout.test.ts` 的「尾巴上挂着未来空位时，兜底读最后一根真实 K」。
- **背景改成方格（纵向网格线）**（用户反馈「背景似乎是一些方格，而我们用的平行线」）：
  引擎默认只画横向网格线（`grid.x: false`），而直接打开 `grid.x` 会得到**每个类目一条竖线**
  —— 类目轴的 `scale.ticks()` 无视参数、返回整个 domain，视窗里 120 根就是 120 条线
  （实测截图：一片栅栏）。所以先在上游补了通用能力（见 `ice-chart` CHANGELOG 的同名条目）：
  ① 垂直网格线跟 **x 轴标签抽稀同一批位置**；② `xAxis.show: false` 的图**也出这张抽稀表**
  （只是不占排版空间、Axis 不画）—— 多 pane 里上面几块常藏掉 x 轴，网格却要和下面那块对齐。
  本仓示例页三块 pane 开 `grid: { x: true }`，于是竖线数量 = 时间标签数量、三块 pane 的竖线
  落在同一批 x 上，和横向刻度交叉成方格。回归网：上游 `tests/chart/grid-x.test.ts`；
  本仓 e2e「背景是方格：垂直网格线跟时间标签同一批位置，三块 pane 对齐」（在未来空位里
  扫像素数竖线，并逐个比三块 pane 的竖线 x）。
- **报价精度（两位小数）**（用户反馈「我希望你带上小数，两位小数」）：新增
  `TradingChartExtras.pricePrecision` —— 给了就按固定位数格式化**价格轴刻度**与**提示框里的四个价**
  （`41800.00`，不再去尾随 0：价格轴要的是一列对齐的报价）；不给则保持引擎原来的自适应写法。
  `formatPrice(value, precision?)` 与 `DEFAULT_PRICE_PRECISION = 2` 一并导出。示例页传 2，
  页面自己画的价签 / 准星价签 / 抬头 OHLC 全部同口径（e2e 逐处断言两位小数）。
  两个连带项：
  - pane 栈里 **`toTradingOption` 必须排在 `alignAxes` 之前** —— 定宽 formatter 是包在外层的，
    顺序反了会让 `pricePrecision` 看到「已经有 formatter」而让路，小数永远加不上；
  - 刻度标签变长，**补位预算 `axisLabelChars` 要跟着加**（`41800.00` 是 8 字符、六位数价格 9），
    示例页 8 → 9，三块 pane 的绘图区仍然等宽（e2e 盯着）。
- **数值轴刻度密度按像素给**（用户反馈「K 线右边标尺上的数字那么稀疏」）：引擎的默认档数是
  固定 5 档，287px 高的价格轴上一档 57~71px。现在 pane 栈给**没写 `tickCount`** 的 y 轴注入
  按密度反推的档数（口径照主流看盘软件：一档留出 `字号 × 2.5` 的高度），示例页实测
  价格轴 8~13 档、一档 19~47px，成交量 / 副图各自按自己的轴长给 3~4 档。三件事决定了实现：
  - **不能直接传「想要几档」**：引擎把 `跨度 / 档数` 严格向上取到 1/2/5×10^k，而且**取两次**
    （先取整数据域、再取整刻度），跨 10^k 时一档会直接稀一半（实测跨度 957 / 轴 287：
    档数 11 → 域取整到 1100 → 再取整成 200）。所以 `axisTickCount()` 把整条链路模拟一遍，
    再反查「落点离目标最近」的档数。
  - **按「显示中」的窗口算**：手动量程之后，视图窗口和 option 里的 `min`/`max` 不是一个东西 ——
    拿后者算会得到「窗口已缩到 275 宽、档数还按 501 宽给的 12 档」，一档当场 52px。
    所以密度取 `chart.getAxisDomain(i)`；页面在「只动数值轴」时也会排一次量程重算。
  - **尺寸排完要重算一遍**：密度依赖绘图区高度，而高度只有排完版才知道。
    `createPaneStack` 的 `resize()` 排完尺寸后再应用一遍 option（否则首帧会按画布的默认尺寸
    算档数，容器尺寸变化后也不会更新）。
  新的公开导出：`axisTickCount()` / `DEFAULT_AXIS_TICK_SPACING`。回归网：`tests/panes.test.ts`
  的四条单测 + 建栈后数真实标签，`e2e/terminal-pan.spec.ts` 的「刻度密度」那条。
- **标尺上的三个手势**（主流看盘软件的口径；引擎的交互一律要求指针在绘图区内，标尺那条带子
  只能应用层自己接）：右侧数值标尺**按住上下拖** = 缩放该 pane 的数值轴（向上拖 = 放大）、
  **双击** = 该 pane 的数值轴自适应、**滚轮** = 微调同一个量程（指针指着的价格纹丝不动）；
  底部**时间轴双击** = 重置时间轴（回到默认根数 + 跟盘）。滚轮在绘图区里照旧是时间轴缩放，
  几条路径互不干扰。
- **标尺拖拽缩放**：新的公开导出 `beginValueAxisScale(chart, y)` + `applyValueAxisScale(chart, scale, y)`。
  走**快照**口径：缩放系数 =「按下点到绘图区下缘的距离 / 当前点到绘图区下缘的距离」，两边各加
  `0.2 × 绘图区高度` 做阻尼（指针贴到下缘时系数不会发散），下限 0.1；锚点是按下那一刻窗口的
  中心；每一帧都拿**按下时的窗口**重算，所以拖回出发点就是原样（不累积误差）。上下限仍与滚轮
  同一条口径（`interaction.zoom` 的 `minSpan` / `maxSpan`），夹取走 `setDomain('y', …, 'zoom')`。
  按下时 `setPointerCapture` —— 拖动全程指针会跑出标尺，不抓指针会中途丢事件。
- **标尺光标**：悬停右侧数值标尺是 `ns-resize`（那里按下就是上下拖拽缩放），按下拖动全程保持；
  标尺上的按下**不给平移小手**（`bindPanCursor` 让路）。绘图区仍然守「按住主键才变小手」。
- **刻度精度跟着窗口走**（引擎既有能力的显式回归网）：窗口越细，`niceStep` 给出的步长越小，
  步长掉到 1 以下时标签自动多带小数位（`formatNumberTick`）。示例页实测：拖到很细时价格轴
  步长 100 → 50；同时 e2e 盯着「标签变长也没把三块 pane 的绘图区挤歪」（补位预算 `axisLabelChars`
  顶破就会错位）。
- **数值轴自适应：在右侧数值标尺上双击**。纵向拖过之后量程被
  「手动窗口」钉住 —— 引擎把拖出来的 y 窗口记进视图状态，写在 option 里的 `yAxis.min/max`
  再也进不去，K 线被挤出绘图区也回不来（此前只有一个「重置」，会连数据一起重采样）。
  现在双击该 pane 右侧的标尺（绘图区右缘 → 画布右缘、纵向在绘图区内）就把 y 交还给
  自动量程，**x 窗口一根不动**；三块 pane 各认自己的标尺。
  落地是新的公开导出 `resetAutoScale(chart)`：只走公开 API —— `setOption(…, { preserveView: false })`
  是引擎里唯一能清掉视图状态的入口（`resetZoom()` 同效但带动画、同样会清 x），随后把 x 窗口
  按原样写回去；全程包在 `silent()` 里，所以**不会**把「用户动过视窗 → 退出跟盘」误触发。
  回归网：`tests/axisView.test.ts`（含「清掉之后量程真的跟着 option 走」——防止把它实现成
  「把当前自适应值写成手动窗口」）+ `e2e/terminal-pan.spec.ts` 的标尺用例。
- **数值轴缩放**：新的公开导出 `zoomValueAxis(chart, { factor, anchorY, minSpan?, maxSpan? })` ——
  口径与引擎自己的连续轴缩放一致（新跨度 = 当前跨度 / factor，夹在「完整数据域 × `minSpan` /
  `maxSpan`」之间，上下限优先读 option 里的 `interaction.zoom`），锚点处的价格不动；走
  `setDomain('y', …, 'zoom')` 才和引擎的手势缩放同一条夹取路径。
- **「只动数值轴」不再算「动过视窗」**：引擎只要窗口动了就抛 `pan:change` / `zoom:change`，
  页面此前照单全收 —— 于是纵向拖一下、或在标尺上把价格轴放大一点，跟盘就被踢掉、时间窗口
  停在原处，新 K 掉在视窗外。现在按**视窗指纹**（根数 + 两端 key）判断：指纹没变就不退出跟盘
  （顺带把「一次联动三个 pane 各抛一次事件」去重成一次重算）。页面自己摆窗口的地方
  （`pinToLatest` / `invalidate`）负责刷新指纹。
- **图表外壳的两条边界**（标尺缩放的连带问题）：最新价被推出视野时，那条最新价虚线**不画**
  （它挂在整个 pane 栈的覆盖层上，画出去会落到下面那两块图上），价签**钉在价格轴的上/下沿**；
  指针停在右侧标尺上时**不出准星横线**（此前只判了纵向，悬停标尺会画出一条悬空的横线 + 价签）。
- **缩放有了上下限**（用户反馈「缩放应该有最大和最小比例限制」）：限制交给 `ice-chart`
  新加的 `interaction.zoom.minBarSpacing` / `maxBarSpacing`（px/根 口径，见该包 CHANGELOG），
  示例页用默认值 —— 最密 0.5px/根、最粗半幅一根。实测：缩到底从原来的 **0.24px/根**
  （一根占不到一个像素，整片糊成色带）收在 0.5px/根；放到头约 3 根铺满一屏。
- **缩小时不再凭空长出未来空位**（`anchorRightOnZoom`）：滚轮缩放绕指针锚定，窗口往两边
  一起长 —— 停在最新一根上缩小时右半边会一路长进那 600 个未来空位，画面右半变成一片空白
  （实测缩到底：3565 根里 2400 根是空位）。现在给右端一条闸门：`cap = max(本轮右端, 最新一根)`，
  超了整窗左移（跨度不变）—— 缩小时看到的是**更多历史**，最新那根始终贴在最右边。
  闸门只拦「比之前更靠右」，所以用户自己拖进空位看未来时不会被拽回去（`pan:change` 刷新 cap）。

### 修复

- **示例页的盘口会「穿价」**：伪实时盘口只 upsert 不删除，几十秒后一张 4 万的盘口里攒满了陈价，
  买一高于卖一、价差变成负数。现在深度走「快照 + 增量 + **删除**」：以现价为锚滚动十档，
  离开窗口的档按协议用 `size: 0` 删掉（买卖两侧各删各的 —— 一个价位从买盘迁到卖盘时旧的那侧要先删）。
- **示例页 `renderAccount()` 调用了一个不存在的方法**（`this.positionMargin()`）：只要有持仓，
  每 500ms 就抛一次错。现在账户面板与持仓表同源，都从仓位 topic 的 store 读；顺带把
  `#o-avail` 被写两遍、还重复带上「可用」前缀的问题一起收掉（标签在 HTML 里，`<b>` 里只放数字）。
- **链路自身的提示不再抢脚注那一行**：深度对账（`resync`）原先写 `setHint()`，于是随机丢包会把用户
  刚看到的手势 / 下单反馈顶掉（e2e「底部时间轴上双击」就是这么红的）。现在链路健康归脚注最右那一格
  `#s-feed`（状态 + 重连次数 + 对账计数 + 悬停提示）。

### 新增

- 示例页的 **K 线区域可拖动平移**（横纵都能拖，纵向拖即手动量程）：
  - 光标**只在按住主键时**才是「抓住」的小手：悬停是普通箭头（不给「可抓」，主流看盘
    软件里图表上就是箭头），按下主键立刻变手，松手（哪怕指针已经拖到图外）立刻收回；
    画线工具武装时这一片归画线层的 `crosshair`，不参与；
  - 图上载入 `可见根数 × 3` 的历史，默认仍只显示最后 120 根 —— 不载历史的话
    平移被夹在数据范围内，默认缩放下拖动是**零位移**；
  - **跟盘 / 手动两态**：默认跟盘（新 K 始终钉在右端）；用户拖过或滚轮缩放过就转手动，
    视窗原样保留 —— **图表不再自动向右延伸**，并亮出「回到最新」；重置 / 切周期回到跟盘；
  - 价格 / 成交量 / MACD 的量程改为按**可见窗口**算（否则载入 3 倍历史后 K 线会整体变矮），
    平移/缩放时 rAF 节流重算（实测单帧 ≈ 2.7ms）。
- 右侧「**未来空位**」：视窗停在最新 K 上时向左拖，右端不再是墙，而是一片空白
  （`FUTURE_SLOTS = 600`，拖深了按需再补到 `FUTURE_MAX = 2400`）——
  与左侧「无限画布」对称。空位是**锚在绝对时刻上的类目**（只有 `at` / `x`，没有 OHLC，
  所以什么都不画），新 K 流进来时把已经变成真实 K 的那几个从队首摘掉、队尾补齐，
  **已存在的槽 key 不变** —— 否则用户停在未来时，每来一根新 K 整个视窗都会被顶一下。
  配套三件事（都是实测踩出来的）：
  - 渲染窗口（`renderSlice`）**一定带上最后 `count` 根真实 K**：视窗拖进空位深处后，
    这一段里可能一根真实 K 都没有，量程就没了依据 —— 价格轴掉到 `[-148, 451]`、
    量图掉到 `[0, 1]`、副图掉到 `[-1, 1]`（MACD 的收盘价序列退化成一串 0）；
  - 均线 / MACD / RSI **只喂真实 K**（`indicatorSource`）：它们的输入是收盘价序列，
    空位没有 OHLC 时 `closeSeries` 会沿用上一根收盘，于是指标会在空位里拉出一条平线，
    像「指标跑到未来去了」；
  - 副图再补一个**只带 x、什么都不画**的占位系列（`blankAnchor`）：副图的指标系列虽然
    停在了最后一根真实 K（上面那条），但类目轴是按「这格系列里出现过的 x」建域的 ——
    副图里没有未来那串类目，联动过来的窗口在它那儿找不到右端 key，只能停在老窗口上，
    于是「K 线已经是空白区、副图还在画老数据」，三块 pane 当场错位。
    用 `type: 'bar'` + `value: null` 补类目（量图处理空位用的就是这一手，像素为 0）。
- 时间标签拆成「类目 key」与「显示」两件事：key 带日期保证**全局唯一**，
  显示由 `displayLabel()` 按当前可见跨度决定要不要露日期（一屏不足一天仍是 `HH:MM`）。
  轴刻度、抬头时间、准星时间签三处统一走它。
- 示例页的**十字准星**按主流看盘软件的样子重做：
  - **竖线**由引擎画（`crosshair.axis: 'x'`）并**贯穿三块 pane**（联动同步），是一把时间标尺；
  - **横线**画在**指针所在的那一块**里（页面覆盖层），读数用那一块自己的 y 轴 ——
    在成交量 pane 上读出来就是成交量，在 MACD / RSI pane 上是指标值；
  - **右轴价签**跟指针纵坐标；**时间签**贴在**最下面那块**的时间轴上并按标签宽度居中、
    夹在绘图区内（周/月线的标签比 `HH:MM` 长得多，写死宽度会截断）；
  - 准星价签与最新价签会叠住时，最新价签让位（阈值按标签**实际高度**算）；
  - 关掉引擎的悬停标记（`interaction.hover.mark: false`）：默认会给整列**每个系列各画一个**
    半透明白圆环，一根 K 会连带 MA / BOLL 被圈上、白花花地盖住 K 线。
  - **蜡烛不再有悬停白描边**（`CandlestickSeries` 里删掉了那层 overlay）：准星已经指明
    读的是哪一根，再叠一圈描边只会把正要看的那根 K 盖住。
- 覆盖层改挂在 `#panes`（整摞 pane 的容器）上，坐标原点显式对齐到主图画布左上角
  （`syncChromeBox()`）；指针跟踪也挂在容器上，顺带避开 pane 之间 1px 分隔条导致的准星闪断。
- 三块 pane 的周期表先前已扩到 11 档，本次把准星在各档上都验过（含 1H 的时间签显示
  `09-18 18:46` 这种带日期/年份的标签）。

### 修复

- **量图空档那一根丢了类目 → 后面所有量柱整体错位**：`buildVolumeSeries` 对读不出量的
  数据项推的是 `{ value: null }`（没有 `x`）。类目轴是按「这一份系列里出现过的 x」建域的，
  少一个 x 就少一格：中间空一根，后面所有量柱左移一格；尾巴上连着一片空（比如视窗停在
  最新 K 右边），这一格的类目就整段缺掉，联动过来的窗口在这里找不到右端 key，
  引擎的 clamp 只好退回「整段数据」，量图当场被拉成整幅。现在空档项也带上 x。
  回归用例：`读不出量的那一根**也要带 x**`。
- **纵向拖动方向是反的，而且根本没在平移**：`shiftDomain` 对 x / y 用同一个符号，但
  x 轴「值越大越靠右」与屏幕同向、y 轴「值越大越靠上」与屏幕**反向**，于是向下拖时
  内容往上跑；再加上 `clampAxisDomain` 把窗口与数据范围**求交**（窗口本来就等于数据范围），
  平移被悄悄退化成缩放 —— 实测纵向拖 120px 量程从 `[41100,41700]` 变成 `[41100,41449]`。
  两处都在 `ice-chart` 侧修（见该包 CHANGELOG）。
- **「无限画布」：往左拖不再撞墙**。原先只载 `可见 × 3` 根的固定历史，拖到底就停住
  （用户原话「拖到左侧边缘就无法再拖动了」）。现在内存里存整段历史、往左按需补
  （`ensureHistory` / `prependHistory`），并且**交给图表的只是视窗附近那几百根**
  （`renderSlice`）—— 否则归一化 / 指标 / 图元全是 O(n)，历史一深，拖动每帧一次就顶到
  60fps 预算（实测 360 根 1.8ms → 2760 根 10.7ms）。实测：连拖 12 屏后内存里 1962 根、
  图表只拿 720 根、单帧仍是 1.9ms，历史往前接了 4 天且接缝跳空 0%。
- **十字准星「卡卡的」**：竖线原本交给引擎画，而引擎的准星**吸附到柱心** ——
  慢速横移 61 步（每步 1px）实测：目标位置只变了 9 个值、竖线 60 步里 **53 步位移为 0**、
  剩下 7 步一次跳 8px，即「鼠标在动、线一动不动，攒够了跳一格」。
  改成**两条线都由页面画、自由跟随指针**（`crosshair.show: false`）：位移步长恒为 1px、
  滞后 0px，时间签也跟着指针 x 走（文字取最近那一根）。顺带统一了两条线的虚线节奏，
  并对齐到设备像素（dpr=2 下发虚）。注意这不是性能问题：当时帧间隔 p50 16.7 / p95 17.6，
  一帧没掉、每步 JS 只有 0.2ms。

### 修复（需配套 `ice-chart` 的上游修复，见该包 CHANGELOG）

跨 pane 的准星此前**根本出不来**：被联动回显出悬停的副图，会在同一次 mousemove 里
判定「指针不在我身上」而把回显清掉。上游修了 `ChartLink`（只认源头发的 `item:leave`）
与 `InteractionController.externalHover`（指针只收自己放上去的悬停）之后才成立。

横向拖动还依赖第三个上游修复：**类目轴上的平移原本整条失效**（取窗口时要求
`domain.length === 2`，而类目轴的 `domain` 是窗口内的整串类目；位移计算又把那串当全集）。
症状是「能上下拖、不能左右拖」。

## 0.1.0 - 2026-09-21

首版。交易能力从 `ice-chart` 整体迁出到本包，`ice-chart` 从此不承载金融交易图表。

### 变更

- **K 线（`type: 'candlestick'`）**：作为 ice-chart 的**自定义系列**接入
  （`registerSeriesType`），命中判定 / 悬停高亮 / 提示框 / 图例 / 序列化全部沿用
  ice-chart 既有链路。类型名沿用 ice-chart 0.28 之前的内置名，用户 option 不用改。
- **数据契约**：数据项是 `{ x, o, c, l, h }`（也认 `[开, 收, 低, 高]` 数组、
  也认 `open/close/low/high` 长名）。字段名可用 `openField` / `closeField` /
  `lowField` / `highField` 覆盖。读不出的数据项不画，不补 0。
- **含影线的价格轴量程**：`computePriceRange` 按全量数据的最低价的低点 → 最高价的高点
  算出范围（默认 6% 留白），通过公开的 `yAxis.min` / `yAxis.max` 落下去 ——
  否则 ice-chart 的自动量程只看收盘价，影线会被裁。
  可用 `{ autoPriceRange: false }` 关掉。
- **OHLC 提示框**：`createOhlcTooltipFormatter` 走公开的 `tooltip.formatter`，
  行名「开盘 / 收盘 / 最低 / 最高」可用 `{ priceLabels }` 换；只返回 `rows`，
  标题仍由 ice-chart 按坐标轴格式化给出。
- **时间轴**：默认 `xAxis: { type: 'category' }` —— 等宽 K 线、跳过非交易时段。
- 公开 API：`createTradingChart` / `toTradingOption` / `registerTradingSeries` /
  `readOhlc` / `hasOhlc` / `computePriceRange` / `createOhlcTooltipFormatter` /
  `CandlestickSeries`。
- **成交量副图**：数据项带 `v` 就自动加一个绑到第二 y 轴的 `bar` 系列，轴域钉成
  `[0, ratio × 最大量]` 且 `show:false`，柱子落在绘图区底部 `1/ratio`（默认 20%）。
  可配 `volume: { field, ratio, upColor, downColor, axisIndex, barWidth }`，`volume: false` 关闭。
  柱子按 `close ≥ open` 逐项上色。
- **图表外壳的数据侧**：`createOhlcReadout()` 给出「当前光标那一根」的读数
  （开高低收 / 量 / 涨跌额 / 涨跌幅 / 涨跌方向），供页面画抬头。
- **画布内坐标投影**：`plotRect` / `priceToY` / `yToPrice` / `categoryToX` / `xToCategoryIndex`，
  让页面用 HTML 画的外壳（价签、横线、标签）能与图表精确对齐。
- **格式化**：`formatPrice` / `formatVolume` / `formatSigned` / `formatPct`。
- 提示框默认触发器改为 `axis`（整列读数）—— 与引擎默认一致，抬头跟随十字光标靠它；
  行名与顺序统一为 开 / 高 / 低 / 收（+ 量），量按成交量格式化而不是价格格式化。
- **真副图（pane 栈）**：`createPaneStack` 把容器切成纵向堆叠的多块画布，每块一个 `ICEChart`，
  x 轴类目共享、`linkCharts` 联动 hover / zoom / pan。横向对齐由「等宽字体 + 定宽刻度标签」
  保证（`fixedWidthAxisFormatter`）；更新走 `stack.refresh()`，由栈负责重新注入对齐信息。
- **技术指标**：纯函数 `sma` / `ema` / `stdev` / `bollinger` / `macd` / `rsi`（预热期为 `null`，
  长度与输入一致），以及 `createOverlaySeries`（主图叠加 MA / EMA / BOLL）、
  `createMacdPaneOption`（柱 + DIF + DEA）、`createRsiPaneOption`（RSI + 参考线）、`macdRange`。
  派生系列数据带 `x`，且默认裁掉首尾 `null` —— 引擎会把 null 点当成 0 画出一条竖直假线。
- **画线工具**：`createDrawingLayer` 用 SVG 覆盖层实现水平线 / 垂直线 / 趋势线 / 区间矩形，
  支持选中、拖锚点、整条平移、删除，按**数据坐标**序列化（`dump()` / `load()`），
  缩放平移后按当前比例尺重投影。
- **坐标投影补强**：`xToCategoryIndex` 改用 `scale.invert`（命中不到取最近类目）——
  类目带之间有 20% 空隙，`indexAt` 落在空隙里返回 -1。
- **默认按设备像素比渲染**：引擎的 `dpr` 默认是 1 且不会自己读 `window.devicePixelRatio`，
  不传的话高分屏上整张图是糊的（1x 位图被浏览器放大）。`createTradingChart` /
  `createPaneStack` 现在默认取环境 dpr（上限 3），显式传 `dpr: 1` 可退回旧行为。
- **蜡烛画法重做**（用户反馈「画得很难看」）：
  - 实体改为**纯填充、不描边** —— 同色描边会向外各撑半条线，相邻蜡烛几乎贴在一起；
  - 去掉阳线的半透明（原本 0.55），半透明会让均线从蜡烛里透出来，很脏；
  - 影线改为**实体上下两段**并平头端对，不再从实体中间穿过（空心阳线时那条竖线尤其明显）；
  - 实体边缘与影线对齐设备像素：不传 dpr 时模糊，传了 dpr 又会有 1/3 像素的细线，
    所以线宽统一改按 CSS 像素语义（`cssUnit() = unit() * dpr`）；
  - **影线严格居中**：原先实体左右边缘各自取整、影线单独 `snap()`，两套取整让**根根蜡烛
    都偏 0.5 个设备像素**（用户实测反馈「影线没在蜡烛里居中，好诡异」）。现在绘制与命中
    共用一份 `candleGeometry()`：影线中轴按线宽奇偶落在正确像素上，实体宽取同奇偶的整数
    设备像素并以影线为中轴；
  - 新增 `candle.hollowUp`（阳线空心，国内传统画法），示例里给了开关；
  - **默认实体宽改为「铺满类目带宽」**（`barWidth` 默认 1）：类目轴已经用 `paddingInner`
    扣掉 20% 步距当间隙，实体再乘 0.66 就只剩 53%、一半都是缝（用户反馈「柱之间太稀疏」）。
    主流终端库是「实体占步距的 82%~86%」，铺满带宽即同一量级。成交量柱同样默认铺满 ——
    比蜡烛窄会让副图显得比主图稀疏。
- **盘口组件** `createOrderBook(container, options)`：买卖**十档**（每侧档数可配，默认 10）、
  深度条、中间价与价差、点某一档回调 `onPickPrice`。结构只建一次、按签名跳过无变化的写入、
  自带样式（注入一次）。数据契约为「最优价在前」，卖盘展示时自动倒序。
- 示例的图表顶部工具条按「常显收藏 + 分组下拉」重做：周期（分组 / 星标收藏 / 自定义周期）、
  指标（主图叠加多选 + 副图单选）、画线、显示（涨跌配色 / 阳线空心）；收藏与自定义周期存本地。
  周期从页面顶栏挪到图表顶部。
- **指标预热期**：多生成 40 根只喂指标、不显示（MACD 要 33 根才有第一个柱值），
  消掉副图左侧的空白 —— 与真实终端「多取一段历史」同一个思路。
- 周期表扩到 11 档（加 3m/30m/2H/1W/1M），类目标签按跨度自动补日期/年份，保证逐根唯一。
- 示例：**只保留一个综合示例** `examples/terminal.html`（完整交易终端屏）——
  行情条（含周期切换 1m~1D、涨跌配色切换）＋ 盘口十档（深度渐变、点价填单）＋ 三 pane 真副图
  （价格 / 成交量 / MACD 或 RSI，严格对齐 + 联动）＋ 均线开关 ＋ 画线工具（水平线 / 垂直线 /
  趋势线 / 区间，可拖可删、数据坐标可导出）＋ 最新价线 / 右轴价签 / OHLC 抬头 / 十字光标两侧
  标签 ＋ 下单面板（限价·市价 / 全仓·逐仓 / 杠杆 / 止盈止损 / 只减仓）＋ 持仓表（盈亏、保证金、
  强平价随现价动）＋ 委托表（可撤单、现价穿过自动撮合）＋ 收盘倒计时。
  分页示例（candlestick / panes / drawing）已删除，内容都整合进来了。
- 示例布局改为**图表 · 盘口 · 下单**（盘口从最左移到图表右侧），副图之间改成 **1px 分隔线**
  （原先 6px 间距 + 每块画布上下各 12px 的默认留白，光空隙就吃掉 ~52px），
  成交量轴改为贴着数据（关掉 `nice` 取整，原先 3188 的峰值被抬到 4200，又空出 22px）。

### 其它

- peer 依赖 `@damoqiongqiu/ice-chart@^0.28.0` 与 `ice-render@^4.1.0`；
  `dependencies` 恒为空（家族约定：运行时依赖一律走 peer）。
- 已知边界：价格轴按**全量数据**钉量程，缩放时不自适应。要做「随可见区间自适应」，
  监听 `zoom:change` / `pan:change` 后重算并 `setOption`。
